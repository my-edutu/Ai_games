const test=require('node:test');const assert=require('node:assert/strict');
const {parseSnakeConfig}=require('../../dist/games/autonomous-snake/src/config/schema.js');const {createInitialState}=require('../../dist/games/autonomous-snake/src/index.js');const {NamedRng}=require('../../dist/packages/seeded-rng/src/index.js');const {applyAction,isLegalAction}=require('../../dist/games/autonomous-snake/src/rules/step.js');
test('reversal is rejected and movement is atomic',()=>{const c=parseSnakeConfig({width:8,height:8,targetLength:20});const s=createInitialState(c,'x','r');assert.equal(isLegalAction(s,{direction:'left'}),false);const r=applyAction(s,{direction:'right'},NamedRng.fromSeed('x'));assert.equal(r.state.tick,1);assert.equal(r.state.snake.body.length,s.snake.body.length)});
test('eating grows and increments score',()=>{const c=parseSnakeConfig({width:8,height:8,targetLength:20});const s=createInitialState(c,'eat','r');const head=s.snake.body[0];s.food=head+1;const r=applyAction(s,{direction:'right'},NamedRng.fromSeed('eat'));assert.equal(r.state.snake.body.length,s.snake.body.length+1);assert.equal(r.state.score,1)});
test('wall collision terminates',()=>{const c=parseSnakeConfig({width:5,height:5,targetLength:20});let s=createInitialState(c,'wall','r');s.snake.body=[4,3,2];s.snake.direction='right';const r=applyAction(s,{direction:'right'},NamedRng.fromSeed('wall'));assert.equal(r.state.lifecycle,'result');assert.equal(r.state.result.reason,'wall-collision')});
test('tail vacates when not growing but blocks when growing',()=>{const c=parseSnakeConfig({width:5,height:5,targetLength:20});const s=createInitialState(c,'tail','r');s.snake.body=[7,6,11,12];s.snake.direction='up';s.food=0;assert.equal(isLegalAction(s,{direction:'down'}),false);assert.equal(isLegalAction(s,{direction:'right'}),true)});
test('self collision is classified separately',()=>{const c=parseSnakeConfig({width:6,height:6,targetLength:20});const s=createInitialState(c,'self','r');s.snake.body=[14,13,19,20,21,15];s.snake.direction='right';s.food=0;const r=applyAction(s,{direction:'down'},NamedRng.fromSeed('self'));assert.equal(r.state.result.reason,'self-collision')});
test('target collection resolves victory',()=>{const c=parseSnakeConfig({width:6,height:6,targetLength:4});const s=createInitialState(c,'victory','r');s.food=s.snake.body[0]+1;const r=applyAction(s,{direction:'right'},NamedRng.fromSeed('victory'));assert.equal(r.state.result.reason,'victory');assert.equal(r.state.snake.body.length,4)});
