'use strict';
const fs=require('node:fs');const path=require('node:path');const{test,expect}=require('@playwright/test');const base='http://127.0.0.1:4175',artifacts=path.resolve(__dirname,'../../artifacts/ant-phase3');test.beforeAll(()=>fs.mkdirSync(artifacts,{recursive:true}));
test('ant colony desktop broadcast is animated, readable and privacy safe',async({page})=>{const failures=[];page.on('console',message=>{if(message.type()==='error')failures.push(message.text())});page.on('pageerror',error=>failures.push(error.message));await page.setViewportSize({width:1920,height:1080});await page.goto(`${base}/ant`,{waitUntil:'domcontentloaded'});await expect(page.locator('[data-testid="ant-canvas"]')).toBeVisible();await expect(page.locator('[data-testid="objective"]')).toContainText(/ascension/i);await expect(page.locator('[data-testid="ai-intent"]')).not.toBeEmpty();const first=await page.locator('[data-testid="tick"]').textContent();await page.waitForTimeout(700);const second=await page.locator('[data-testid="tick"]').textContent();expect(second).not.toBe(first);const state=await page.evaluate(()=>window.__ANT_PUBLIC_STATE__);expect(state).toBeTruthy();expect(state.seed).toBeUndefined();expect(state.config).toBeUndefined();expect(state.influence).toBeUndefined();expect(JSON.stringify(state)).not.toContain('appliedIds');expect(JSON.stringify(state)).not.toContain('lastCell');const layout=await page.evaluate(()=>({viewport:document.documentElement.clientWidth,scroll:document.documentElement.scrollWidth,canvas:document.querySelector('[data-testid="ant-canvas"]').getBoundingClientRect().toJSON()}));expect(layout.scroll).toBe(layout.viewport);expect(layout.canvas.width).toBeGreaterThan(900);expect(layout.canvas.height).toBeGreaterThan(700);await page.screenshot({path:path.join(artifacts,'desktop.png'),fullPage:true});expect(failures).toEqual([])});
test('phone landscape retains the colony, primary progress and captions',async({page})=>{await page.setViewportSize({width:640,height:360});await page.goto(`${base}/ant?muted=1`,{waitUntil:'domcontentloaded'});await expect(page.locator('[data-testid="ant-canvas"]')).toBeVisible();await expect(page.locator('[data-testid="objective"]')).toBeVisible();await expect(page.locator('[data-testid="captions"]')).toBeVisible();const box=await page.locator('[data-testid="ant-canvas"]').boundingBox();expect(box.width).toBeGreaterThanOrEqual(400);expect(box.height).toBeGreaterThan(250);const overflow=await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth);expect(overflow).toBe(0);await page.screenshot({path:path.join(artifacts,'phone-landscape.png'),fullPage:true})});
test('clean feed, high contrast and reduced motion preserve the game view while controls stay private',async({page})=>{await page.setViewportSize({width:1280,height:720});await page.goto(`${base}/ant?reducedMotion=1&highContrast=1&muted=1&cleanFeed=1`,{waitUntil:'domcontentloaded'});await expect(page.locator('body')).toHaveAttribute('data-reduced-motion','true');await expect(page.locator('body')).toHaveAttribute('data-high-contrast','true');await expect(page.locator('[data-testid="ant-canvas"]')).toBeVisible();await expect(page.locator('[data-testid="hud"]')).toBeHidden();await expect(page.locator('#operator-panel')).toBeHidden();const box=await page.locator('[data-testid="ant-canvas"]').boundingBox();expect(Math.round(box.width)).toBe(1280);expect(Math.round(box.height)).toBe(720);await page.screenshot({path:path.join(artifacts,'clean-feed.png'),fullPage:true});await page.goto(`${base}/ant?controls=1&muted=1`,{waitUntil:'domcontentloaded'});await expect(page.locator('#operator-panel')).toBeVisible()});
