'use strict';
const fs=require('node:fs');const path=require('node:path');const{test,expect}=require('@playwright/test');const base='http://127.0.0.1:4175',artifacts=path.resolve(__dirname,'../../artifacts/escape-room-phase3');test.beforeAll(()=>fs.mkdirSync(artifacts,{recursive:true}));
test('escape room desktop broadcast is animated, readable and privacy safe',async({page})=>{const failures=[];page.on('console',message=>{if(message.type()==='error')failures.push(message.text())});page.on('pageerror',error=>failures.push(error.message));await page.setViewportSize({width:1920,height:1080});await page.goto(`${base}/escape-room`,{waitUntil:'domcontentloaded'});await expect(page.locator('[data-testid="escape-canvas"]')).toBeVisible();await expect(page.locator('[data-testid="objective"]')).toContainText(/escape/i);await expect(page.locator('[data-testid="ai-intent"]')).not.toBeEmpty();const first=await page.locator('[data-testid="tick"]').textContent();await page.waitForTimeout(700);const second=await page.locator('[data-testid="tick"]').textContent();expect(second).not.toBe(first);const state=await page.evaluate(()=>window.__ESCAPE_PUBLIC_STATE__);expect(state).toBeTruthy();expect(state.seed).toBeUndefined();expect(state.runId).toBeUndefined();expect(state.roomSeed).toBeUndefined();expect(JSON.stringify(state)).not.toMatch(/hiddenFact|solution|oracle/i);const layout=await page.evaluate(()=>({viewport:document.documentElement.clientWidth,scroll:document.documentElement.scrollWidth,canvas:document.querySelector('[data-testid="escape-canvas"]').getBoundingClientRect().toJSON()}));expect(layout.scroll).toBe(layout.viewport);expect(layout.canvas.width).toBeGreaterThan(1000);expect(layout.canvas.height).toBeGreaterThan(600);await page.screenshot({path:path.join(artifacts,'desktop.png'),fullPage:true});expect(failures).toEqual([])});
test('phone landscape retains vault, progress, captions and intent',async({page})=>{await page.setViewportSize({width:844,height:390});await page.goto(`${base}/escape-room`,{waitUntil:'domcontentloaded'});await expect(page.locator('[data-testid="escape-canvas"]')).toBeVisible();await expect(page.locator('[data-testid="progress"]')).toBeVisible();await expect(page.locator('[data-testid="captions"]')).toBeVisible();await expect(page.locator('[data-testid="ai-intent"]')).toBeVisible();const box=await page.locator('[data-testid="escape-canvas"]').boundingBox();expect(box.width).toBeGreaterThan(480);expect(box.height).toBeGreaterThan(250);await page.screenshot({path:path.join(artifacts,'phone-landscape.png'),fullPage:true})});
test('reduced motion, high contrast, mute and clean feed preserve game truth',async({page})=>{await page.setViewportSize({width:1280,height:720});await page.goto(`${base}/escape-room?reducedMotion=1&highContrast=1&muted=1&cleanFeed=1`,{waitUntil:'domcontentloaded'});await expect(page.locator('body')).toHaveAttribute('data-reduced-motion','true');await expect(page.locator('body')).toHaveAttribute('data-high-contrast','true');await expect(page.locator('[data-testid="escape-canvas"]')).toBeVisible();await expect(page.locator('[data-testid="captions"]')).toBeVisible();await expect(page.locator('[data-testid="hud"]')).toBeHidden();await page.screenshot({path:path.join(artifacts,'clean-feed.png'),fullPage:true})});
