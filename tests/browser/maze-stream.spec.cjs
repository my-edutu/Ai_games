'use strict';
const fs=require('node:fs');const path=require('node:path');const{test,expect}=require('@playwright/test');const base='http://127.0.0.1:4174',artifacts=path.resolve(__dirname,'../../artifacts/maze-phase3');test.beforeAll(()=>fs.mkdirSync(artifacts,{recursive:true}));
test('maze desktop broadcast is animated, readable and privacy safe',async({page})=>{const failures=[];page.on('console',message=>{if(message.type()==='error')failures.push(message.text())});page.on('pageerror',error=>failures.push(error.message));await page.setViewportSize({width:1920,height:1080});await page.goto(`${base}/maze`,{waitUntil:'domcontentloaded'});await expect(page.locator('[data-testid="maze-canvas"]')).toBeVisible();await expect(page.locator('[data-testid="objective"]')).toContainText(/escape/i);await expect(page.locator('[data-testid="ai-intent"]')).not.toBeEmpty();const first=await page.locator('[data-testid="tick"]').textContent();await page.waitForTimeout(600);const second=await page.locator('[data-testid="tick"]').textContent();expect(second).not.toBe(first);const state=await page.evaluate(()=>window.__MAZE_PUBLIC_STATE__);expect(state).toBeTruthy();expect(state.seed).toBeUndefined();expect(state.runId).toBeUndefined();expect(state.world).toBeUndefined();expect(state.passages).toBeUndefined();expect(JSON.stringify(state)).not.toContain('shortestSolutionLength');const layout=await page.evaluate(()=>({viewport:document.documentElement.clientWidth,scroll:document.documentElement.scrollWidth,canvas:document.querySelector('[data-testid="maze-canvas"]').getBoundingClientRect().toJSON()}));expect(layout.scroll).toBe(layout.viewport);expect(layout.canvas.width).toBeGreaterThan(1000);expect(layout.canvas.height).toBeGreaterThan(500);await page.screenshot({path:path.join(artifacts,'desktop.png'),fullPage:true});expect(failures).toEqual([])});
test('phone landscape retains maze, progress, captions and legible controls',async({page})=>{await page.setViewportSize({width:844,height:390});await page.goto(`${base}/maze`,{waitUntil:'domcontentloaded'});await expect(page.locator('[data-testid="maze-canvas"]')).toBeVisible();await expect(page.locator('[data-testid="progress"]')).toBeVisible();await expect(page.locator('[data-testid="captions"]')).toBeVisible();const box=await page.locator('[data-testid="maze-canvas"]').boundingBox();expect(box.width).toBeGreaterThan(480);expect(box.height).toBeGreaterThan(250);await page.screenshot({path:path.join(artifacts,'phone-landscape.png'),fullPage:true})});
test('reduced motion, high contrast, mute and clean feed preserve the game view',async({page})=>{await page.setViewportSize({width:1280,height:720});await page.goto(`${base}/maze?reducedMotion=1&highContrast=1&muted=1&cleanFeed=1`,{waitUntil:'domcontentloaded'});await expect(page.locator('body')).toHaveAttribute('data-reduced-motion','true');await expect(page.locator('body')).toHaveAttribute('data-high-contrast','true');await expect(page.locator('[data-testid="maze-canvas"]')).toBeVisible();await expect(page.locator('[data-testid="captions"]')).toBeVisible();await expect(page.locator('[data-testid="hud"]')).toBeHidden();await page.screenshot({path:path.join(artifacts,'clean-feed.png'),fullPage:true})});
