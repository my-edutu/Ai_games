import{checksum}from'../../../../packages/replay/src/index';import{NamedRng}from'../../../../packages/seeded-rng/src/index';import{parseTowerConfig,type TowerConfig}from'../config/schema';import{createInitialTowerState,type TowerPolicyMode}from'../index';import{applyTowerAction,makeTowerTerminal}from'../rules/step';import type{TowerAction,TowerEvent,TowerPlatform,TowerState}from'../state/types';
export interface TowerRuntimeOptions{policy?:TowerPolicyMode}
function oracleAction(state:TowerState):TowerAction{const chunk=state.chunks.find(c=>c.floor===state.floor)??state.chunks[state.chunks.length-1],p=state.player,platforms=[...chunk.platforms].sort((a,b)=>a.y-b.y||a.id.localeCompare(b.id)),bottom=p.position.y-p.halfHeight,candidates=platforms.filter(x=>x.y+x.height>bottom+5000);let target:TowerPlatform|undefined;for(const c of candidates){const center=c.x+c.width/2,dx=Math.abs(center-p.position.x);if(dx<190000){target=c;break}}if(!target)return{move:0,jump:p.grounded,dash:false,attack:false,ability:false};const center=target.x+target.width/2,dx=center-p.position.x,move:TowerAction['move']=Math.abs(dx)<9000?0:dx>0?1:-1,vertical=target.y+target.height-bottom,jump=p.grounded&&(Math.abs(dx)<125000||vertical<50000);return{move,jump,dash:false,attack:false,ability:false}}
export class TowerRuntime{public events:TowerEvent[]=[];public state:TowerState;public rng:NamedRng;private seq:number;private constructor(public config:TowerConfig,public seed:string,public readonly policy:TowerPolicyMode,state?:TowerState,rng?:NamedRng,nextEventSeq=0,events:TowerEvent[]=[]){this.seq=nextEventSeq;this.rng=rng??NamedRng.fromSeed(seed);this.state=state??createInitialTowerState(config,seed,`tower-run-${checksum({seed,floor:config.launchFloor})}`,this.rng);this.events=events.map(e=>structuredClone(e));if(!state)this.emit('initialized',{seed,policy})}static create(config:Partial<TowerConfig>,seed:string,options:TowerRuntimeOptions={}){return new TowerRuntime(parseTowerConfig(config),seed,options.policy??'autonomous')}static restore(config:TowerConfig,seed:string,policy:TowerPolicyMode,state:TowerState,rng:NamedRng,nextEventSeq=0,events:TowerEvent[]=[]){return new TowerRuntime(config,seed,policy,state,rng,nextEventSeq,events)}private emit(type:string,data?:Record<string,unknown>,tick=this.state.tick){this.events.push({seq:this.seq++,tick,type,data})}private chooseAction(){this.state.ai.decisions++;if(this.policy==='idle-test'){this.state.ai.mode='idle-test';this.state.ai.intent='Holding position for lifecycle verification';return{move:0,jump:false,dash:false,attack:false,ability:false}as TowerAction}const action=oracleAction(this.state);this.state.ai.mode=this.policy==='oracle-test'?'oracle-test':'climbing';this.state.ai.intent=action.jump?'Committing to the next platform':action.move?'Aligning with the next landing':'Waiting for a safe launch';this.state.ai.confidence=.9;return action}step(action?:Partial<TowerAction>){if(this.state.lifecycle==='result'){this.state=structuredClone(this.state);this.state.lifecycle='intermission';this.state.intermissionRemaining=this.config.intermissionTicks;this.emit('intermission');return this.state}if(this.state.lifecycle==='intermission'){this.state=structuredClone(this.state);this.state.intermissionRemaining--;if(this.state.intermissionRemaining<=0)this.restart(`${this.seed}:restart:${this.state.tick}`);return this.state}const selected=action??this.chooseAction(),out=applyTowerAction(this.state,selected,this.rng);this.state=out.state;if(!out.accepted){this.state.stats.invalidActions++;this.emit('action-rejected',{reason:out.reason??'unknown'})}for(const e of out.events)this.emit(e.type,e.data,e.tick);if(!this.state.result&&this.state.tick>=this.config.maxTicks){this.state=makeTowerTerminal(this.state,'timer-expired');this.emit('game-terminal',{reason:'timer-expired'})}else if(!this.state.result&&this.state.tick-this.state.meaningfulEventTick>=this.config.noProgressTicks){this.state=makeTowerTerminal(this.state,'stagnation');this.emit('game-terminal',{reason:'stagnation'})}this.state.ai.recentHeights.push(this.state.height);if(this.state.ai.recentHeights.length>64)this.state.ai.recentHeights.shift();this.emit('ai-intent',{mode:this.state.ai.mode,intent:this.state.ai.intent});return this.state}restart(seed=this.seed){const restarts=this.state.stats.restarts+1;this.seed=seed;this.rng=NamedRng.fromSeed(seed);this.state=createInitialTowerState(this.config,seed,`tower-run-${checksum({seed,floor:this.config.launchFloor})}`,this.rng);this.state.stats.restarts=restarts;this.emit('restart',{seed});return this.state}drainEvents(limit=10000){if(!Number.isInteger(limit)||limit<0)throw new RangeError('limit');return this.events.splice(0,Math.min(limit,this.events.length))}getNextEventSequence(){return this.seq}}
