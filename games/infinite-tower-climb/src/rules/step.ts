import{checksum}from'../../../../packages/replay/src/index';import{NamedRng}from'../../../../packages/seeded-rng/src/index';import{generateTowerChunk}from'../generation/chunks';import{validateTowerChunk}from'../generation/validator';import{stepTowerPhysics}from'../physics/step';import type{TowerAction,TowerEvent,TowerResultReason,TowerState}from'../state/types';
export interface TowerRuleResult{state:TowerState;events:Omit<TowerEvent,'seq'>[];accepted:boolean;reason?:string}
export function normalizeTowerAction(action:Partial<TowerAction>|undefined):TowerAction{const move=action?.move??0;if(move!==-1&&move!==0&&move!==1)throw new RangeError('move');return{move,jump:Boolean(action?.jump),dash:Boolean(action?.dash),attack:Boolean(action?.attack),ability:Boolean(action?.ability)}}
export function makeTowerTerminal(input:TowerState,reason:TowerResultReason,kind:'game'|'technical'|'operator'='game'){const state=structuredClone(input);state.player.state='dead';state.lifecycle='result';const base={kind,reason,tick:state.tick,floor:state.floor,height:state.stats.maxHeight};state.result={...base,checksum:checksum(base)};if(kind==='technical')state.stats.technicalOutcomes++;return state}
export function applyTowerAction(input:TowerState,actionInput:Partial<TowerAction>|undefined,rng:NamedRng):TowerRuleResult{if(input.lifecycle!=='running')return{state:structuredClone(input),events:[],accepted:false,reason:'not-running'};let action:TowerAction;try{action=normalizeTowerAction(actionInput)}catch{return{state:structuredClone(input),events:[],accepted:false,reason:'invalid-action'}}const stepped=stepTowerPhysics(input,action),events:Omit<TowerEvent,'seq'>[]=[];let state=stepped.state;state.tick++;if(stepped.integrityFailure){state=makeTowerTerminal(state,'integrity-quarantine','technical');events.push({tick:state.tick,type:'technical-terminal',data:{reason:stepped.integrityFailure}});return{state,events,accepted:true}}for(const contact of stepped.contacts){events.push({tick:state.tick,type:`contact-${contact.kind}`,data:{entityId:contact.entityId}});if(contact.kind==='hazard'&&state.player.invulnerableTicks===0){state.player.health--;state.player.invulnerableTicks=20;state.player.state='hurt';state.stats.hazardHits++;events.push({tick:state.tick,type:'damage',data:{source:contact.entityId,health:state.player.health}})}}const current=state.chunks.find(c=>c.floor===state.floor)??state.chunks[state.chunks.length-1];if(state.player.health<=0){state=makeTowerTerminal(state,'health-depleted');events.push({tick:state.tick,type:'game-terminal',data:{reason:'health-depleted'}})}else if(state.player.position.y<current.baseY-state.config.killDepth){state.stats.falls++;state=makeTowerTerminal(state,'fall');events.push({tick:state.tick,type:'game-terminal',data:{reason:'fall'}})}else if(state.player.position.y>=current.exitY){const nextFloor=state.floor+1,next=generateTowerChunk(state.config,state.seed,nextFloor,rng),validation=validateTowerChunk(next,state.config);if(!validation.valid){state=makeTowerTerminal(state,'integrity-quarantine','technical');events.push({tick:state.tick,type:'technical-terminal',data:{reason:'invalid-generated-chunk',issues:validation.issues}})}else{state.floor=nextFloor;state.stats.floorsCleared++;state.stats.checkpoints++;state.player.position={...next.spawn};state.player.velocity={x:0,y:0};state.player.grounded=false;state.player.groundedPlatformId=undefined;state.player.checkpoint={...next.spawn};state.player.checkpointFloor=nextFloor;state.player.health=Math.min(state.player.maxHealth,state.player.health+1);state.chunks.push(next);state.chunks=state.chunks.filter(c=>c.floor>=nextFloor-state.config.visibleChunkCount+1);state.meaningfulEventTick=state.tick;events.push({tick:state.tick,type:'floor-complete',data:{floor:nextFloor,theme:next.theme,guardian:next.guardian}})}}return{state,events,accepted:true}}
