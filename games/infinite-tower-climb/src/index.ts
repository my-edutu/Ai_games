import{checksum}from'../../../packages/replay/src/index';import{NamedRng}from'../../../packages/seeded-rng/src/index';import type{TowerConfig}from'./config/schema';import{generateTowerChunk}from'./generation/chunks';import{generateTowerContent}from'./generation/content';import{validateTowerChunk}from'./generation/validator';import{createInitialTowerBuild}from'./progression/builds';import{createInitialTowerInfluenceState}from'./influence/types';import type{TowerState}from'./state/types';
export type TowerPolicyMode='autonomous'|'oracle-test'|'idle-test';
export function createInitialTowerState(config:TowerConfig,seed:string,runId=`tower-run-${checksum({seed,floor:config.launchFloor})}`,rng=NamedRng.fromSeed(seed)):TowerState{const chunk=generateTowerChunk(config,seed,config.launchFloor,rng),validation=validateTowerChunk(chunk,config);if(!validation.valid)throw Object.assign(new Error(`invalid initial chunk: ${validation.issues.join(',')}`),{code:'INVALID_TOWER_CHUNK'});const content=generateTowerContent(chunk,config,seed,rng);return{schemaVersion:1,runId,seed,config,tick:0,lifecycle:'running',intermissionRemaining:config.intermissionTicks,floor:config.launchFloor,height:chunk.spawn.y,meaningfulEventTick:0,chunks:[chunk],player:{id:'climber',position:{...chunk.spawn},velocity:{x:0,y:0},halfWidth:config.playerHalfWidth,halfHeight:config.playerHalfHeight,grounded:false,facing:1,health:5,maxHealth:5,stamina:100,maxStamina:100,dashCooldown:0,attackCooldown:0,abilityCooldown:0,invulnerableTicks:12,shieldCharges:0,checkpoint:{...chunk.spawn},checkpointFloor:config.launchFloor,state:'airborne',jumpCharges:1,score:0},enemies:content.enemies,projectiles:[],pickups:content.pickups,build:createInitialTowerBuild(),pendingUpgradeOffers:[],influence:createInitialTowerInfluenceState(),stats:{floorsCleared:0,maxHeight:chunk.spawn.y,jumps:0,dashes:0,falls:0,hazardHits:0,invalidActions:0,restarts:0,checkpoints:0,technicalOutcomes:0,enemiesDefeated:0,guardiansDefeated:0,damageDealt:0,damageTaken:0,pickupsCollected:0,upgradesApplied:0,stuckRecoveries:0,projectilesFired:0},ai:{mode:'boot',intent:'Assessing the first route',confidence:1,fallbackCount:0,decisions:0,recentHeights:[],nodeExpansions:0}}}
export*from'./config/schema';export*from'./state/types';export*from'./generation/chunks';export*from'./generation/content';export*from'./generation/validator';export*from'./generation/solver';export*from'./physics/step';export*from'./combat/step';export*from'./progression/builds';export*from'./ai/observation';export*from'./ai/graph';export*from'./ai/policy';export*from'./ai/stuck';export*from'./influence/index';export*from'./rules/step';export*from'./runtime/run';export*from'./persistence/snapshot';export*from'./testing/headless';export*from'./testing/campaign';
