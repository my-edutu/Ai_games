{
  "name": "@ai-games/ai-zombie-survival",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "serve": "npm run build && node serve.mjs"
  }
}
