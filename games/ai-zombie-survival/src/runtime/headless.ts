import type{ZombieConfig}from'../config/schema';import{ZombieRuntime}from'./run';
export interface ZombieHeadlessOptions{seed:string;config:Partial<ZombieConfig>;ticks:number}
export interface ZombieHeadlessSummary{seed:string;ticks:number;results:number;restarts:number;evacuations:number;overruns:number;quarantines:number;highestDay:number;finalChecksum:string;retainedEvents:number}
export function runZombieHeadless(options:ZombieHeadlessOptions):ZombieHeadlessSummary{if(!Number.isInteger(options.ticks)||options.ticks<0)throw new RangeError('ticks');const runtime=ZombieRuntime.create(options.config,options.seed);let highestDay=runtime.state.day;for(let i=0;i<options.ticks;i++){runtime.step();highestDay=Math.max(highestDay,runtime.state.day)}const results=runtime.events.filter(event=>event.type==='run-result');return{seed:options.seed,ticks:options.ticks,results:results.length,restarts:runtime.events.filter(event=>event.type==='run-restarted').length,evacuations:results.filter(event=>event.data?.outcome==='evacuated').length,overruns:results.filter(event=>event.data?.outcome==='overrun').length,quarantines:results.filter(event=>event.data?.outcome==='quarantined').length,highestDay,finalChecksum:runtime.stateChecksum(),retainedEvents:runtime.events.length}}
export function runZombieCampaign(options:{seeds:string[];config:Partial<ZombieConfig>;ticks:number}){const seeds=[...options.seeds],runs=seeds.map(seed=>runZombieHeadless({seed,config:options.config,ticks:options.ticks}));let divergences=0;for(let index=0;index<seeds.length;index++){const repeat=runZombieHeadless({seed:seeds[index],config:options.config,ticks:options.ticks});if(JSON.stringify(repeat)!==JSON.stringify(runs[index]))divergences++}return{seeds,runs,divergences,totalResults:runs.reduce((sum,run)=>sum+run.results,0),totalRestarts:runs.reduce((sum,run)=>sum+run.restarts,0)}}
