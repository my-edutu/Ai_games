import type{RngSnapshot}from'../../../../packages/seeded-rng/src/index';import{checksum}from'../../../../packages/replay/src/index';import{assertZombieInvariants}from'../rules/step';import{ZombieRuntime}from'../runtime/run';import type{ZombieEvent,ZombieState}from'../state/types';
export interface ZombieSnapshot{schemaVersion:1;gameId:'ai-zombie-survival';deterministicVersion:'zombie-v1';seed:string;tick:number;payload:ZombieState;rng:RngSnapshot;nextEventSequence:number;events:ZombieEvent[];checksum:string}
function material(snapshot:Omit<ZombieSnapshot,'checksum'>){return{payload:snapshot.payload,rng:snapshot.rng,nextEventSequence:snapshot.nextEventSequence}}
export function createZombieSnapshot(runtime:ZombieRuntime):ZombieSnapshot{const base:Omit<ZombieSnapshot,'checksum'>={schemaVersion:1,gameId:'ai-zombie-survival',deterministicVersion:'zombie-v1',seed:runtime.seed,tick:runtime.state.tick,payload:structuredClone(runtime.state),rng:runtime.rng.snapshot(),nextEventSequence:runtime.getNextEventSequence(),events:runtime.events.map(e=>structuredClone(e))};return{...base,checksum:checksum(material(base))}}
export function restoreZombieRuntime(snapshot:ZombieSnapshot){if(snapshot.schemaVersion!==1)throw new Error('unsupported schemaVersion');if(snapshot.gameId!=='ai-zombie-survival')throw new Error('gameId mismatch');if(snapshot.deterministicVersion!=='zombie-v1')throw new Error('deterministicVersion mismatch');const expected=checksum(material(snapshot));if(expected!==snapshot.checksum)throw new Error('snapshot checksum mismatch');assertZombieInvariants(snapshot.payload);if(snapshot.payload.seed!==snapshot.seed)throw new Error('seed mismatch');if(snapshot.payload.tick!==snapshot.tick)throw new Error('tick mismatch');return ZombieRuntime.restore({config:snapshot.payload.config,seed:snapshot.seed,state:snapshot.payload,rng:snapshot.rng,nextEventSequence:snapshot.nextEventSequence,events:snapshot.events})}
