import type{NamedRng}from'../../../../packages/seeded-rng/src/index';import type{ZombieConfig}from'../config/schema';import type{GeneratedZombieWorld,ResourceKind,ZombieWorld,ZombieWorldFeatures}from'../state/types';import{fromCell,reachable,shortestPathLength,toCell}from'../rules/grid';
const kinds:ResourceKind[]=['materials','ammo','medicine','food','power'];
function baseCells(config:ZombieConfig){const cx=Math.floor(config.width/2),cy=Math.floor(config.height/2),cells:number[]=[];for(let y=cy-2;y<=cy+2;y++)for(let x=cx-3;x<=cx+3;x++)cells.push(toCell(x,y,config.width));return cells.sort((a,b)=>a-b)}
function candidateResourceCell(kindIndex:number,config:ZombieConfig,rng:NamedRng,used:Set<number>,base:Set<number>){const anchors=[[3,3],[config.width-4,3],[3,config.height-4],[config.width-4,config.height-4],[Math.floor(config.width/2),3]];const[ax,ay]=anchors[kindIndex];for(let attempt=0;attempt<32;attempt++){const dx=rng.nextInt(`resources:${kindIndex}:x`,5)-2,dy=rng.nextInt(`resources:${kindIndex}:y`,5)-2;const x=Math.max(1,Math.min(config.width-2,ax+dx)),y=Math.max(1,Math.min(config.height-2,ay+dy)),cell=toCell(x,y,config.width);if(!used.has(cell)&&!base.has(cell)){used.add(cell);return cell}}for(let cell=0;cell<config.width*config.height;cell++)if(!used.has(cell)&&!base.has(cell)){used.add(cell);return cell}throw new Error('resource placement exhausted')}
function createWorld(config:ZombieConfig,rng:NamedRng,withObstacles:boolean):ZombieWorld{const base=baseCells(config),baseSet=new Set(base),cx=Math.floor(config.width/2),cy=Math.floor(config.height/2);const gates=[{id:'gate-north',cell:toCell(cx,0,config.width),side:'north' as const},{id:'gate-east',cell:toCell(config.width-1,cy,config.width),side:'east' as const},{id:'gate-south',cell:toCell(cx,config.height-1,config.width),side:'south' as const},{id:'gate-west',cell:toCell(0,cy,config.width),side:'west' as const}];const used=new Set<number>([...base,...gates.map(g=>g.cell)]);const resourceSites=kinds.map((kind,index)=>({id:`site-${kind}`,kind,cell:candidateResourceCell(index,config,rng,used,baseSet),stock:100}));const reserved=new Set<number>([...used]);const blocked:number[]=[];if(withObstacles){const desired=Math.min(config.obstacleCount,Math.floor(config.width*config.height/5));for(let attempt=0;attempt<desired*8&&blocked.length<desired;attempt++){const cell=rng.nextInt('world:obstacles',config.width*config.height),{x,y}=fromCell(cell,config.width);if(x===0||y===0||x===config.width-1||y===config.height-1||reserved.has(cell))continue;blocked.push(cell);reserved.add(cell)}}return{schemaVersion:1,width:config.width,height:config.height,baseCells:base,coreCell:toCell(cx,cy,config.width),gates,resourceSites,blockedCells:blocked.sort((a,b)=>a-b)}}
export function validateZombieWorld(world:ZombieWorld,config:ZombieConfig){const errors:string[]=[];if(world.width!==config.width||world.height!==config.height)errors.push('dimension mismatch');if(new Set(world.baseCells).size!==world.baseCells.length)errors.push('duplicate base cell');if(world.gates.length!==4)errors.push('gate count');if(world.resourceSites.length!==5)errors.push('resource count');if(new Set(world.resourceSites.map(s=>s.cell)).size!==world.resourceSites.length)errors.push('duplicate resource');const blocked=new Set(world.blockedCells),base=new Set(world.baseCells);for(const gate of world.gates)if(shortestPathLength(gate.cell,base,world.width,world.height,blocked)<0)errors.push(`unreachable ${gate.id}`);for(const site of world.resourceSites){if(base.has(site.cell))errors.push(`resource in base ${site.id}`);if(blocked.has(site.cell))errors.push(`resource blocked ${site.id}`);if(!reachable(site.cell,world.coreCell,world.width,world.height,blocked))errors.push(`resource unreachable ${site.id}`)}return errors}
function features(world:ZombieWorld):ZombieWorldFeatures{const blocked=new Set(world.blockedCells),base=new Set(world.baseCells),routes=world.gates.map(g=>shortestPathLength(g.cell,base,world.width,world.height,blocked));return{routeLengthMin:Math.min(...routes),routeLengthMax:Math.max(...routes),bottlenecks:world.blockedCells.length,coverDensityPermille:Math.floor(world.blockedCells.length*1000/(world.width*world.height)),resourceRisk:world.resourceSites.reduce((sum,site)=>sum+shortestPathLength(site.cell,new Set([world.coreCell]),world.width,world.height,blocked),0)}}
export function generateZombieWorld(config:ZombieConfig,rng:NamedRng):GeneratedZombieWorld{const failedReasons:string[]=[];for(let attempt=1;attempt<=config.generationAttempts;attempt++){const world=createWorld(config,rng,true),errors=validateZombieWorld(world,config);if(errors.length===0)return{world,features:features(world),diagnostics:{attempts:attempt,repairs:0,fallbackUsed:false,failedReasons}};failedReasons.push(...errors)}const world=createWorld(config,rng,false),errors=validateZombieWorld(world,config);if(errors.length)throw new Error(`fallback invalid: ${errors.join(',')}`);return{world,features:features(world),diagnostics:{attempts:config.generationAttempts,repairs:1,fallbackUsed:true,failedReasons}}}
