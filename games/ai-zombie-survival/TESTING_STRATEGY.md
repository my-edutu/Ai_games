# Testing Strategy

Node's built-in test runner verifies deterministic replay, visible survivor action mapping, zombie spacing, barricade damage/repair, day/night phase progression, bounded/idempotent viewer influence, detached render snapshots, camera priority, presentation pose mapping, 250-zombie sustained simulation, infection progression and deterministic reinforcement hordes.

Run `npm test` from this directory. Browser runtime verification should additionally capture the required scene evidence in an environment that permits local browser navigation.
