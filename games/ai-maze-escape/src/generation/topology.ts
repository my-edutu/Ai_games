import type{MazeConfig}from '../../../../packages/game-contracts/src/index';import{NamedRng}from '../../../../packages/seeded-rng/src/index';import type{GeneratedMaze,MazeFeatures,MazeWorld}from '../state/types';import{populateMazeContent,solveMazeWithInventory,validateMazeContent}from './content';import{PASSAGE_DOWN,PASSAGE_LEFT,PASSAGE_RIGHT,PASSAGE_UP,passageNeighbors,solveMaze}from './solver';import{validateGeneratedMaze}from './validator';
const dirs=[[-1,0,PASSAGE_UP,PASSAGE_DOWN],[0,1,PASSAGE_RIGHT,PASSAGE_LEFT],[1,0,PASSAGE_DOWN,PASSAGE_UP],[0,-1,PASSAGE_LEFT,PASSAGE_RIGHT]]as const;
function connect(passages:number[],a:number,b:number,width:number){const ar=Math.floor(a/width),ac=a%width,br=Math.floor(b/width),bc=b%width;if(br===ar-1&&bc===ac){passages[a]|=PASSAGE_UP;passages[b]|=PASSAGE_DOWN}else if(br===ar+1&&bc===ac){passages[a]|=PASSAGE_DOWN;passages[b]|=PASSAGE_UP}else if(br===ar&&bc===ac+1){passages[a]|=PASSAGE_RIGHT;passages[b]|=PASSAGE_LEFT}else if(br===ar&&bc===ac-1){passages[a]|=PASSAGE_LEFT;passages[b]|=PASSAGE_RIGHT}else throw new Error('non-adjacent connection')}
function farthest(world:MazeWorld,start:number){const cells=world.width*world.height,dist=new Int32Array(cells);dist.fill(-1);const queue=[start];dist[start]=0;let best=start;for(let q=0;q<queue.length;q++){const current=queue[q];if(dist[current]>dist[best]||dist[current]===dist[best]&&current<best)best=current;for(const next of passageNeighbors(world,current))if(dist[next]===-1){dist[next]=dist[current]+1;queue.push(next)}}return best}
function buildWorld(config:MazeConfig,rng:NamedRng,attempt:number):MazeWorld{const cells=config.width*config.height,passages=new Array<number>(cells).fill(0),visited=new Uint8Array(cells),start=rng.nextInt(`world-generation:${attempt}:start`,cells),stack=[start];visited[start]=1;while(stack.length){const current=stack[stack.length-1],row=Math.floor(current/config.width),col=current%config.width,candidates:number[]=[];for(const[dr,dc]of dirs){const nr=row+dr,nc=col+dc;if(nr>=0&&nr<config.height&&nc>=0&&nc<config.width){const next=nr*config.width+nc;if(!visited[next])candidates.push(next)}}if(!candidates.length){stack.pop();continue}const next=candidates[rng.nextInt(`world-generation:${attempt}:walk`,candidates.length)];connect(passages,current,next,config.width);visited[next]=1;stack.push(next)}for(let cell=0;cell<cells;cell++){const row=Math.floor(cell/config.width),col=cell%config.width;for(const[dr,dc]of [[0,1],[1,0]]as const){const nr=row+dr,nc=col+dc;if(nr>=config.height||nc>=config.width)continue;const next=nr*config.width+nc;if(passages[cell]&(dr===0?PASSAGE_RIGHT:PASSAGE_DOWN))continue;let chance=config.loopChancePermille;if(config.profile==='chambers'&&row%4!==0&&col%4!==0)chance=Math.min(1000,chance+240);if(config.profile==='layers'&&row%5===0)chance=Math.min(1000,chance+180);if(rng.nextInt(`world-generation:${attempt}:loops`,1000)<chance)connect(passages,cell,next,config.width)}}const provisional:MazeWorld={schemaVersion:1,width:config.width,height:config.height,passages,start,exit:start,doors:[],keys:[],traps:[],clues:[],checkpoints:[],threats:[],blockedCells:[],profile:config.profile};provisional.exit=farthest(provisional,start);return provisional}
function fallbackWorld(config:MazeConfig):MazeWorld{const cells=config.width*config.height,passages=new Array<number>(cells).fill(0);for(let r=0;r<config.height;r++){const forward=r%2===0;for(let c=0;c<config.width-1;c++){const left=r*config.width+(forward?c:config.width-1-c),right=r*config.width+(forward?c+1:config.width-2-c);connect(passages,left,right,config.width)}if(r<config.height-1){const edge=r*config.width+(forward?config.width-1:0);connect(passages,edge,edge+config.width,config.width)}}return{schemaVersion:1,width:config.width,height:config.height,passages,start:0,exit:cells-1,doors:[],keys:[],traps:[],clues:[],checkpoints:[],threats:[],blockedCells:[],profile:config.profile}}
function features(world:MazeWorld):MazeFeatures{let degreeSum=0,deadEnds=0,branchCells=0;for(let cell=0;cell<world.passages.length;cell++){const d=passageNeighbors(world,cell).length;degreeSum+=d;if(d===1)deadEnds++;if(d>=3)branchCells++}const edges=degreeSum/2,solution=solveMazeWithInventory(world),fallback=solveMaze(world,{start:world.start,goal:world.exit});return{profile:world.profile,cells:world.passages.length,edges,loops:Math.max(0,edges-world.passages.length+1),deadEnds,branchCells,shortestSolutionLength:solution?solution.cells.length-1:fallback?fallback.length-1:0,chambers:world.profile==='chambers'?Math.max(1,Math.floor(world.passages.length/48)):0,dependencyDepth:world.doors.length,trapCount:world.traps.length,threatCount:world.threats.length}}
function finalize(base:MazeWorld,config:MazeConfig,rng:NamedRng){const world=populateMazeContent(base,config,rng),validation=validateMazeContent(world,{minimumThreatResponseDistance:Math.min(3,config.visibilityRadius+1)});return{world,validation}}
export function generateMaze(config:MazeConfig,rng:NamedRng):GeneratedMaze{const failedReasons:string[]=[];for(let attempt=1;attempt<=config.repairAttempts;attempt++){try{const base=buildWorld(config,rng,attempt),baseValidation=validateGeneratedMaze(base);if(!baseValidation.valid){failedReasons.push(...baseValidation.reasons.map(reason=>`attempt-${attempt}:${reason}`));continue}const{world,validation}=finalize(base,config,rng);if(validation.valid)return{world,features:features(world),diagnostics:{attempts:attempt,fallbackUsed:false,failedReasons}};failedReasons.push(...validation.reasons.map(reason=>`attempt-${attempt}:${reason}`))}catch(error){failedReasons.push(`attempt-${attempt}:${error instanceof Error?error.message:'unknown'}`)}}const base=fallbackWorld(config),{world,validation}=finalize(base,config,rng);if(!validation.valid)throw new Error(`known-good fallback invalid:${validation.reasons.join(',')}`);return{world,features:features(world),diagnostics:{attempts:config.repairAttempts,fallbackUsed:true,failedReasons}}}
