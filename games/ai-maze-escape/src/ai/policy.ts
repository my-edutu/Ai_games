import type{MazeAction,MazeIntentMode}from '../../../../packages/game-contracts/src/index';import type{MazeObservation}from './observation';import type{MazeBeliefState,MazeState}from '../state/types';import{findKnownPath}from './pathing';import{detectMazeCycle}from './stuck';
export interface MazeDecision{action:MazeAction;intent:{mode:MazeIntentMode;confidence:number;explanation:string;nodeExpansions:number;fallbackUsed:boolean};plannedPath:number[];targetCell:number|null}
function chooseEvasion(observation:MazeObservation,belief:MazeBeliefState):MazeDecision|null{const threats=observation.threats;if(!threats.length)return null;const current=belief.cells[String(observation.currentCell)],legal=current?.neighbors??[],trapCells=new Set(Object.values(belief.cells).filter(cell=>cell.trap||cell.blocked).map(cell=>cell.cell)),threatCells=new Set(threats.map(threat=>threat.cell)),candidates=legal.filter(cell=>!trapCells.has(cell)&&!threatCells.has(cell)&&!belief.cells[String(cell)]?.blocked);if(!candidates.length)return{action:{kind:'wait'},intent:{mode:'evading-threat',confidence:.5,explanation:'No safe observed passage; holding position for the next threat phase.',nodeExpansions:legal.length,fallbackUsed:true},plannedPath:[observation.currentCell],targetCell:null};const score=(cell:number)=>Math.min(...threats.map(threat=>Math.abs(cell-threat.cell)));const target=[...candidates].sort((a,b)=>score(b)-score(a)||(belief.cells[String(a)]?.visits??0)-(belief.cells[String(b)]?.visits??0)||a-b)[0];return{action:{kind:'move',targetCell:target},intent:{mode:'evading-threat',confidence:.92,explanation:'A visible threat is close; moving through the safest observed passage.',nodeExpansions:legal.length,fallbackUsed:false},plannedPath:[observation.currentCell,target],targetCell:target}}
function targetDecision(observation:MazeObservation,belief:MazeBeliefState,target:number,mode:MazeIntentMode,explanation:string,allowUnknownGoal=false):MazeDecision|null{const path=findKnownPath(belief,observation.currentCell,target,{inventory:observation.inventory,avoidCells:Object.values(belief.cells).filter(cell=>cell.trap||cell.blocked).map(cell=>cell.cell),allowUnknownGoal});if(!path||path.path.length<2)return null;return{action:{kind:'move',targetCell:path.path[1]},intent:{mode,confidence:.85,explanation,nodeExpansions:path.expansions,fallbackUsed:false},plannedPath:path.path,targetCell:target}}
export function chooseMazeAction(state:MazeState,observation:MazeObservation,belief:MazeBeliefState):MazeDecision{const evasion=chooseEvasion(observation,belief);if(evasion)return evasion;const visibleKey=observation.keys.filter(key=>!key.collected&&!observation.inventory.includes(key.id)).sort((a,b)=>a.cell-b.cell)[0];if(visibleKey){const decision=targetDecision(observation,belief,visibleKey.cell,'returning-key','A required key is known; navigating to collect it.');if(decision)return decision}if(observation.exitCell!==null){const decision=targetDecision(observation,belief,observation.exitCell,'searching-exit','The exit is visible in the belief map; following a legal known route.');if(decision)return decision}
  const cyclic=detectMazeCycle(state.ai.recentCells),frontierCandidates=belief.frontiers.map(frontier=>({frontier,path:findKnownPath(belief,observation.currentCell,frontier,{inventory:observation.inventory,avoidCells:Object.values(belief.cells).filter(cell=>cell.trap||cell.blocked).map(cell=>cell.cell),allowUnknownGoal:true})})).filter(item=>item.path&&item.path.path.length>=2).sort((a,b)=>(a.path?.path.length??Infinity)-(b.path?.path.length??Infinity)||a.frontier-b.frontier);
  if(frontierCandidates.length){const selected=cyclic&&frontierCandidates.length>1?frontierCandidates[1]:frontierCandidates[0],path=selected.path!;return{action:{kind:'move',targetCell:path.path[1]},intent:{mode:cyclic?'revising-map':'exploring',confidence:cyclic?.72:.8,explanation:cyclic?'The recent route repeated; switching to a different observed frontier.':'Expanding the nearest reachable frontier using only discovered passages.',nodeExpansions:path.expansions,fallbackUsed:false},plannedPath:path.path,targetCell:selected.frontier}}
  const knownExit=Object.values(belief.cells).find(cell=>cell.exit);if(knownExit){const decision=targetDecision(observation,belief,knownExit.cell,'searching-exit','All reachable frontiers are exhausted; returning to the remembered exit.');if(decision)return decision}
  const current=belief.cells[String(observation.currentCell)],previous=state.ai.recentCells.at(-2),fallback=(current?.neighbors??[]).filter(cell=>!belief.cells[String(cell)]?.trap).sort((a,b)=>(belief.cells[String(a)]?.visits??0)-(belief.cells[String(b)]?.visits??0)||Number(a===previous)-Number(b===previous)||a-b)[0];if(fallback!==undefined)return{action:{kind:'move',targetCell:fallback},intent:{mode:'fallback',confidence:.45,explanation:'No reachable frontier or objective route remains; taking the least-visited legal passage.',nodeExpansions:current?.neighbors.length??0,fallbackUsed:true},plannedPath:[observation.currentCell,fallback],targetCell:fallback};return{action:{kind:'wait'},intent:{mode:'fallback',confidence:.2,explanation:'No legal observed move is available; waiting without using hidden information.',nodeExpansions:0,fallbackUsed:true},plannedPath:[observation.currentCell],targetCell:null}}
