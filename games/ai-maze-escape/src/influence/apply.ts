import type{NamedRng}from '../../../../packages/seeded-rng/src/index';import{checksum}from '../../../../packages/replay/src/index';import{validateMazeContent}from '../generation/content';import type{MazeEvent,MazeState}from '../state/types';import{mazeInfluenceCatalogue}from './catalogue';import{applyMazeCandidatePreview}from './candidates';import type{MazeInfluenceCandidate,MazeInfluenceCommand,MazeInfluenceRecord,MazeInfluenceSource}from './types';
function trim(state:MazeState){if(state.influence.seenIds.length>state.influence.maxApplied)state.influence.seenIds.splice(0,state.influence.seenIds.length-state.influence.maxApplied);const allowed=new Set(state.influence.seenIds);for(const id of Object.keys(state.influence.applied))if(!allowed.has(id))delete state.influence.applied[id]}
export function createMazeInfluenceCommand(state:MazeState,input:{id:string;candidate:MazeInfluenceCandidate;scheduledTick:number;expiresAtTick:number;source:MazeInfluenceSource}):MazeInfluenceCommand{if(!input.id||!input.candidate.valid)throw new RangeError('command');if(!Number.isInteger(input.scheduledTick)||!Number.isInteger(input.expiresAtTick)||input.expiresAtTick<input.scheduledTick)throw new RangeError('command ticks');return{schemaVersion:1,id:input.id,effectId:input.candidate.effectId,candidateId:input.candidate.id,payload:structuredClone(input.candidate.payload),issuedAtTick:state.tick,scheduledTick:input.scheduledTick,expiresAtTick:input.expiresAtTick,source:input.source,recordCategory:'audience-influenced'}}
export function queueMazeInfluence(state:MazeState,command:MazeInfluenceCommand):{status:'queued'|'duplicate'|'expired'|'terminal'|'capacity';reason:string}{if(state.influence.applied[command.id]||state.influence.queued.some(item=>item.id===command.id)||state.influence.seenIds.includes(command.id))return{status:'duplicate',reason:'idempotency'};if(state.lifecycle!=='running')return{status:'terminal',reason:'lifecycle'};if(command.expiresAtTick<state.tick)return{status:'expired',reason:'expired'};if(state.influence.queued.length>=state.influence.maxQueued)return{status:'capacity',reason:'queue-capacity'};state.influence.queued.push(structuredClone(command));state.influence.queued.sort((a,b)=>a.scheduledTick-b.scheduledTick||a.id.localeCompare(b.id));state.influence.seenIds.push(command.id);trim(state);return{status:'queued',reason:'queued'}}
function record(state:MazeState,value:MazeInfluenceRecord){state.influence.applied[value.id]=value;if(!state.influence.seenIds.includes(value.id))state.influence.seenIds.push(value.id);trim(state)}
export function applyDueMazeInfluence(state:MazeState,_rng:NamedRng):{state:MazeState;events:Omit<MazeEvent,'seq'>[];applied:number;expired:number;rejected:number}{const events:Omit<MazeEvent,'seq'>[]=[],future:MazeInfluenceCommand[]=[];let applied=0,expired=0,rejected=0;for(const command of state.influence.queued){if(state.influence.applied[command.id])continue;if(command.scheduledTick>state.tick){future.push(command);continue}if(command.expiresAtTick<state.tick){record(state,{id:command.id,effectId:command.effectId,candidateId:command.candidateId,tick:state.tick,status:'expired',source:command.source,applicationCount:0,reason:'expired'});events.push({tick:state.tick,type:'audience-effect-expired',data:{commandId:command.id,effectId:command.effectId}});expired++;continue}const clone=structuredClone(state);applyMazeCandidatePreview(clone,command.effectId,command.payload);const validation=validateMazeContent(clone.world,{minimumThreatResponseDistance:2});if(!validation.valid){record(state,{id:command.id,effectId:command.effectId,candidateId:command.candidateId,tick:state.tick,status:'rejected',source:command.source,applicationCount:0,reason:validation.reasons.join(',')});events.push({tick:state.tick,type:'audience-effect-rejected',data:{commandId:command.id,effectId:command.effectId}});rejected++;continue}applyMazeCandidatePreview(state,command.effectId,command.payload);state.influence.recordCategory='audience-influenced';state.meaningfulEventTick=state.tick;record(state,{id:command.id,effectId:command.effectId,candidateId:command.candidateId,tick:state.tick,status:'applied',source:command.source,applicationCount:1});events.push({tick:state.tick,type:'audience-effect-applied',data:{commandId:command.id,effectId:command.effectId,candidateId:command.candidateId,recordCategory:'audience-influenced'}});applied++}state.influence.queued=future;return{state,events,applied,expired,rejected}}
export function reverseMazeInfluence(state:MazeState,input:{id:string;targetCommandId:string;tick:number}):{status:'reversed'|'duplicate'|'missing'|'refused-irreversible'}{if(state.influence.applied[input.id]||state.influence.seenIds.includes(input.id))return{status:'duplicate'};const queued=state.influence.queued.find(item=>item.id===input.targetCommandId);if(queued){state.influence.queued=state.influence.queued.filter(item=>item.id!==input.targetCommandId);record(state,{id:input.id,effectId:'reversal',candidateId:checksum({target:input.targetCommandId}),tick:input.tick,status:'reversed',source:'reversal',applicationCount:1,targetCommandId:input.targetCommandId});return{status:'reversed'}}const target=state.influence.applied[input.targetCommandId];if(!target)return{status:'missing'};if(target.effectId==='reversal'||!mazeInfluenceCatalogue[target.effectId].reversible){record(state,{id:input.id,effectId:'reversal',candidateId:checksum({target:input.targetCommandId}),tick:input.tick,status:'refused-irreversible',source:'reversal',applicationCount:1,targetCommandId:input.targetCommandId,reason:'irreversible'});return{status:'refused-irreversible'}}if(target.effectId==='fog-pulse')state.influence.fogUntilTick=input.tick;if(target.effectId==='directional-hint'){state.influence.hintUntilTick=input.tick;state.influence.hintPath=[]}if(target.effectId==='threat-pulse'){state.influence.threatPauseUntilTick=input.tick;for(const threat of state.world.threats)threat.pausedUntilTick=input.tick}if(target.effectId==='next-profile')state.influence.nextProfile=null;record(state,{id:input.id,effectId:'reversal',candidateId:checksum({target:input.targetCommandId}),tick:input.tick,status:'reversed',source:'reversal',applicationCount:1,targetCommandId:input.targetCommandId});return{status:'reversed'}}
