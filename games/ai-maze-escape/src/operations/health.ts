import{AlertEngine,MetricRegistry}from '../../../../packages/observability/src/index';
import{RunSupervisor,type RecoveryAction}from '../../../../packages/supervisor/src/index';

export interface MazeOperationalSample{nowMs:number;simulationProgressSeq:number;lastMeaningfulEventMs:number;lastSnapshotMs:number;lastFrameChangeMs:number;lastAudioMs:number;intendedSilence:boolean;luma:number;expectedScene:string;actualScene:string;queueUtilization:number;memorySlopeMbPerHour:number;resourcePressure:number}
export interface MazeOperationalHealth{status:'healthy'|'degraded'|'unsafe';reasons:string[];actions:RecoveryAction[];publicCopy:string;alerts:Array<{type:string;id:string;severity:string;runbook:string}>}
export class MazeOperationsMonitor{private supervisor=new RunSupervisor({heartbeatTimeoutMs:3000,progressTimeoutMs:8000,crashThreshold:3,crashWindowMs:60000,breakerCooldownMs:30000,maxComponents:8});private metrics=new MetricRegistry({maxSeries:64,maxLabelLength:40});private alerts=new AlertEngine([{id:'maze-output-unsafe',metric:'maze_output_unsafe',operator:'gte',threshold:1,forSamples:1,recoverSamples:2,severity:'page',runbook:'docs/operations/ai-maze-escape-runbook.md'},{id:'maze-queue-pressure',metric:'maze_queue_utilization',operator:'gte',threshold:.85,forSamples:2,recoverSamples:2,severity:'ticket',runbook:'docs/operations/ai-maze-escape-runbook.md'}]);private lastProgressSeq=0;private lastProgressMs=0;heartbeat(component:'simulation'|'renderer'|'audio'|'gateway'|'persistence'|'capture',nowMs:number,progressSeq:number,resourcePressure=0){this.supervisor.heartbeat({component,nowMs,progressSeq,resourcePressure})}recordCrash(component:'simulation'|'renderer'|'audio'|'gateway'|'persistence'|'capture',nowMs:number){this.supervisor.recordCrash(component,nowMs)}canRestart(component:string,nowMs:number){return this.supervisor.canRestart(component,nowMs)}recordRecoverySuccess(component:string){this.supervisor.recordRecoverySuccess(component)}observe(sample:MazeOperationalSample):MazeOperationalHealth{if(sample.simulationProgressSeq>this.lastProgressSeq){this.lastProgressSeq=sample.simulationProgressSeq;this.lastProgressMs=sample.nowMs}this.heartbeat('simulation',sample.nowMs,sample.simulationProgressSeq,sample.resourcePressure);this.heartbeat('renderer',sample.nowMs,sample.simulationProgressSeq,sample.resourcePressure);this.heartbeat('audio',sample.nowMs,sample.simulationProgressSeq,0);const reasons:string[]=[],actions:RecoveryAction[]=[];const frameAge=sample.nowMs-sample.lastFrameChangeMs,snapshotAge=sample.nowMs-sample.lastSnapshotMs,audioAge=sample.nowMs-sample.lastAudioMs,meaningfulAge=sample.nowMs-sample.lastMeaningfulEventMs;if(snapshotAge>1500)reasons.push('stale-public-snapshot');if(frameAge>1500)reasons.push('frozen-output');if(sample.luma<=.02)reasons.push('black-output');if(sample.expectedScene!==sample.actualScene)reasons.push('wrong-scene');if(!sample.intendedSilence&&audioAge>5000)reasons.push('silent-output');if(sample.queueUtilization>=.8)reasons.push('queue-pressure');if(sample.memorySlopeMbPerHour>=32)reasons.push('memory-slope');if(meaningfulAge>120000)reasons.push('long-progress-drought');if(frameAge>5000||snapshotAge>5000||(sample.luma<=.02&&frameAge>3000)||reasons.filter(reason=>['black-output','frozen-output','wrong-scene','silent-output'].includes(reason)).length>=3){actions.push({type:'safe-scene',reason:'unsafe-public-output'},{type:'restart-component',component:'renderer',reason:'unsafe-public-output'},{type:'verified-recovery',component:'simulation',reason:'unsafe-public-output'})}else if(reasons.includes('stale-public-snapshot')||reasons.includes('frozen-output'))actions.push({type:'restart-component',component:'renderer',reason:'degraded-output'});if(reasons.includes('silent-output'))actions.push({type:'mute-audio',component:'audio',reason:'audio-unavailable'});if(reasons.includes('queue-pressure')||reasons.includes('memory-slope'))actions.push({type:'reduce-quality',reason:'resource-pressure'});const supervisor=this.supervisor.evaluate(sample.nowMs);reasons.push(...supervisor.reasons);actions.push(...supervisor.actions);const unsafe=reasons.some(reason=>['stale-public-snapshot','frozen-output','black-output','wrong-scene'].includes(reason)&&frameAge>5000)||supervisor.level==='unsafe'||sample.queueUtilization>=1||sample.memorySlopeMbPerHour>=64;this.metrics.observe('maze_output_unsafe',unsafe?1:0,{game:'maze'});this.metrics.observe('maze_queue_utilization',sample.queueUtilization,{game:'maze'});this.metrics.observe('maze_memory_slope_mb_hour',sample.memorySlopeMbPerHour,{game:'maze'});const alerts=this.alerts.evaluate({maze_output_unsafe:unsafe?1:0,maze_queue_utilization:sample.queueUtilization},sample.nowMs);const unique=[...new Map(actions.map(action=>[`${action.type}:${action.component??''}:${action.reason}`,action])).values()];const status=unsafe?'unsafe':reasons.length?'degraded':'healthy';return{status,reasons:[...new Set(reasons)].sort(),actions:unique,publicCopy:status==='healthy'?'Live':status==='degraded'?'Restoring live maze view':'Live output temporarily protected',alerts}}
  snapshot(){return{metrics:this.metrics.snapshot(),supervisor:this.supervisor.snapshot(),activeAlerts:this.alerts.active()}}
}

export type MazeRecoveryWorkflowState='monitoring'|'safe-scene'|'restarting'|'restoring'|'verifying'|'resumed'|'halted';
export class MazeOutputRecoveryWorkflow{private stateValue:MazeRecoveryWorkflowState='monitoring';private attempts=0;private reasons:string[]=[];constructor(private maxAttempts=3){}begin(result:MazeOperationalHealth){this.reasons=[...result.reasons];this.attempts=0;this.stateValue=result.status==='unsafe'?'safe-scene':result.status==='degraded'?'restarting':'resumed';return this.snapshot()}advance(signal:{componentRestarted?:boolean;snapshotVerified?:boolean;outputHealthy?:boolean}){if(this.stateValue==='halted'||this.stateValue==='resumed')return this.snapshot();if(this.stateValue==='safe-scene'){this.stateValue=signal.componentRestarted?'restoring':'restarting';return this.snapshot()}if(this.stateValue==='restarting'){if(signal.componentRestarted)this.stateValue='restoring';return this.snapshot()}if(this.stateValue==='restoring'){if(signal.snapshotVerified)this.stateValue='verifying';else{this.attempts++;if(this.attempts>=this.maxAttempts)this.stateValue='halted'}return this.snapshot()}if(this.stateValue==='verifying'){if(signal.snapshotVerified&&signal.outputHealthy)this.stateValue='resumed';else{this.attempts++;this.stateValue=this.attempts>=this.maxAttempts?'halted':'restoring'}return this.snapshot()}return this.snapshot()}snapshot(){return{state:this.stateValue,attempts:this.attempts,reasons:[...this.reasons]}}}
