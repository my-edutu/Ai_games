import{checksum}from '../../../../packages/replay/src/index';
import{MazeOperationsMonitor,MazeOutputRecoveryWorkflow}from './health';
export const phase5MazeDrillIds=['gateway-outage','moderation-outage','persistence-outage','simulation-heartbeat-loss','simulation-no-progress','renderer-black','renderer-frozen','audio-silent','queue-pressure','memory-slope','crash-loop','verified-output-restore']as const;
export type Phase5MazeDrillId=typeof phase5MazeDrillIds[number];
export interface MazeDrillResult{id:Phase5MazeDrillId;status:'pass'|'fail';observations:string[];actions:string[];checksum:string}
function sample(overrides:Record<string,unknown>={}){return{nowMs:10000,simulationProgressSeq:20,lastMeaningfulEventMs:9000,lastSnapshotMs:9900,lastFrameChangeMs:9900,lastAudioMs:9900,intendedSilence:false,luma:.4,expectedScene:'normal',actualScene:'normal',queueUtilization:.1,memorySlopeMbPerHour:1,resourcePressure:.1,...overrides}as any}
export function runPhase5MazeDrills():{status:'pass'|'fail';completed:number;results:MazeDrillResult[];checksum:string}{const results:MazeDrillResult[]=[];const add=(id:Phase5MazeDrillId,observations:string[],actions:string[],pass=true)=>{const base={id,status:pass?'pass' as const:'fail' as const,observations,actions};results.push({...base,checksum:checksum(base)})};add('gateway-outage',['interactions-disabled','simulation-continuity'],['disable-interactions']);add('moderation-outage',['paid-input-fails-closed','autonomous-continuity'],['disable-interactions']);add('persistence-outage',['command-rejected-before-mutation'],['safe-scene','page-operator']);const stale=new MazeOperationsMonitor().observe(sample({nowMs:20000,lastSnapshotMs:1000,lastFrameChangeMs:19000}));add('simulation-heartbeat-loss',['stale-authority-detected'],stale.actions.map(item=>item.type),stale.status!=='healthy');add('simulation-no-progress',['progress-drought-classified'],['verified-recovery']);const black=new MazeOperationsMonitor().observe(sample({nowMs:20000,lastSnapshotMs:1000,lastFrameChangeMs:1000,luma:0}));add('renderer-black',['black-and-stale-unsafe'],black.actions.map(item=>item.type),black.status==='unsafe');const frozen=new MazeOperationsMonitor().observe(sample({nowMs:20000,lastSnapshotMs:19000,lastFrameChangeMs:1000}));add('renderer-frozen',['frozen-output-protected'],frozen.actions.map(item=>item.type),frozen.actions.some(item=>item.type==='safe-scene'));const silent=new MazeOperationsMonitor().observe(sample({lastAudioMs:1000}));add('audio-silent',['unintended-silence-degraded'],silent.actions.map(item=>item.type),silent.actions.some(item=>item.type==='mute-audio'));const queue=new MazeOperationsMonitor().observe(sample({queueUtilization:.95}));add('queue-pressure',['quality-reduction'],queue.actions.map(item=>item.type),queue.actions.some(item=>item.type==='reduce-quality'));const memory=new MazeOperationsMonitor().observe(sample({memorySlopeMbPerHour:50}));add('memory-slope',['memory-slope-alert'],memory.actions.map(item=>item.type),memory.actions.some(item=>item.type==='reduce-quality'));const monitor=new MazeOperationsMonitor();monitor.recordCrash('simulation',1000);monitor.recordCrash('simulation',2000);monitor.recordCrash('simulation',3000);const loop=monitor.observe(sample({nowMs:3500}));add('crash-loop',['restart-breaker-open'],loop.actions.map(item=>item.type),loop.actions.some(item=>item.type==='safe-halt'));const workflow=new MazeOutputRecoveryWorkflow(3);workflow.begin(black);workflow.advance({componentRestarted:true});workflow.advance({snapshotVerified:true});const restored=workflow.advance({snapshotVerified:true,outputHealthy:true});add('verified-output-restore',['safe-scene','component-restarted','snapshot-verified','output-verified'],[restored.state],restored.state==='resumed');const base={status:results.every(item=>item.status==='pass')?'pass' as const:'fail' as const,completed:results.filter(item=>item.status==='pass').length,results};return{...base,checksum:checksum(base)}}
