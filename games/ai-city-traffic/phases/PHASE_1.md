# Phase 1 — Deterministic Foundation

**Exit status:** complete.

Implemented connected seeded city generation, integer lane-cell vehicles, legal fixed signals, stable propose/resolve/commit movement, bounded demand, collision and route invariants, typed cycle/gridlock results, automatic intermission/restart, checksums, headless execution, and six focused tests.

Evidence command: `npm run test:traffic:phase1` and `npm run traffic:headless`.

Published gate: `e826985a31173e51b01ed6fda09bb7eea890cd53`.
