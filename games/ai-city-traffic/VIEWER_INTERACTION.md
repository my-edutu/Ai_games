# Viewer Interaction

The fixed catalogue contains north–south green wave, east–west green wave, transit priority, bounded demand surge, incident relief, and night theme. Inputs require a normalized schema, stable command ID, tokenized viewer reference, authentication, moderation approval, fixed effect ID, source, logical schedule, and expiry.

Exactly-once records, cooldowns, conflict groups, per-viewer windows, queue caps, run caps, deterministic vote ties, reversals, and public-safe acknowledgements apply equally to free and paid-eligible inputs. Viewer input cannot directly set lifecycle, score, record, gridlock, or cycle completion.
