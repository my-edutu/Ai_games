import type{TrafficCity}from '../state/types';
export interface RouteDecision{laneIds:string[];expansions:number;reachable:boolean}
export function findShortestRoute(city:TrafficCity,origin:string,destination:string,maxExpansions=512):RouteDecision{if(origin===destination)return{laneIds:[],expansions:0,reachable:true};const outgoing=new Map<string,string[]>(),lanes=new Map(city.lanes.map(l=>[l.id,l]));for(const intersection of city.intersections)outgoing.set(intersection.id,intersection.outgoing);const queue=[origin],previous=new Map<string,{intersection:string;laneId:string}>(),seen=new Set<string>([origin]);let expansions=0;while(queue.length&&expansions<maxExpansions){const current=queue.shift()!;expansions++;for(const id of outgoing.get(current)??[]){const lane=lanes.get(id)!;if(seen.has(lane.to))continue;seen.add(lane.to);previous.set(lane.to,{intersection:current,laneId:id});if(lane.to===destination){const route:string[]=[];let cursor=destination;while(cursor!==origin){const step=previous.get(cursor);if(!step)return{laneIds:[],expansions,reachable:false};route.push(step.laneId);cursor=step.intersection}route.reverse();return{laneIds:route,expansions,reachable:true}}queue.push(lane.to)}}return{laneIds:[],expansions,reachable:false}}
