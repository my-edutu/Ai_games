import type{TrafficConfig}from '../../../../packages/game-contracts/src/index';import type{NamedRng}from '../../../../packages/seeded-rng/src/index';import type{TrafficAxis,TrafficCity,TrafficDirection,TrafficIntersection,TrafficLane}from '../state/types';
function intersectionId(x:number,y:number){return`i-${y}-${x}`}function laneId(from:string,to:string){return`lane:${from}>${to}`}function direction(ax:number,ay:number,bx:number,by:number):TrafficDirection{if(by<ay)return'north';if(by>ay)return'south';if(bx>ax)return'east';return'west'}
function classify(config:TrafficConfig,ax:number,ay:number,bx:number,by:number,arterialRow:number,arterialColumn:number):TrafficLane['roadClass']{const perimeter=(ay===0&&by===0)||(ay===config.height-1&&by===config.height-1)||(ax===0&&bx===0)||(ax===config.width-1&&bx===config.width-1);if((config.profile==='ring'||config.profile==='mixed')&&perimeter)return'ring';const arterial=ay===arterialRow&&by===arterialRow||ax===arterialColumn&&bx===arterialColumn;if((config.profile==='arterial'||config.profile==='mixed')&&arterial)return'arterial';return'local'}
export function generateTrafficCity(config:TrafficConfig,rng:NamedRng):TrafficCity{const arterialRow=1+rng.nextInt('city-generation',Math.max(1,config.height-2)),arterialColumn=1+rng.nextInt('city-generation',Math.max(1,config.width-2));const intersections:TrafficIntersection[]=[];for(let y=0;y<config.height;y++)for(let x=0;x<config.width;x++)intersections.push({id:intersectionId(x,y),x,y,incoming:[],outgoing:[],signal:{axis:(x+y)%2===0?'ns':'ew',changedAtTick:0,decisionCount:0,reason:'fixed-cycle initialization'}});const lanes:TrafficLane[]=[];const addPair=(ax:number,ay:number,bx:number,by:number)=>{const a=intersectionId(ax,ay),b=intersectionId(bx,by),axis:TrafficAxis=ax===bx?'ns':'ew',roadClass=classify(config,ax,ay,bx,by,arterialRow,arterialColumn),baseCost=roadClass==='arterial'?Math.max(1,config.laneLength-1):roadClass==='ring'?config.laneLength:config.laneLength+1;for(const[from,to,fx,fy,tx,ty]of[[a,b,ax,ay,bx,by],[b,a,bx,by,ax,ay]]as const)lanes.push({id:laneId(from,to),from,to,axis,direction:direction(fx,fy,tx,ty),length:config.laneLength,roadClass,baseCost,blockedUntilTick:0})};for(let y=0;y<config.height;y++)for(let x=0;x<config.width;x++){if(x+1<config.width)addPair(x,y,x+1,y);if(y+1<config.height)addPair(x,y,x,y+1)}lanes.sort((a,b)=>a.id.localeCompare(b.id));const byId=new Map(intersections.map(i=>[i.id,i]));for(const lane of lanes){byId.get(lane.from)!.outgoing.push(lane.id);byId.get(lane.to)!.incoming.push(lane.id)}for(const item of intersections){item.incoming.sort();item.outgoing.sort()}const gateways=intersections.filter(i=>i.x===0||i.y===0||i.x===config.width-1||i.y===config.height-1).map(i=>i.id).sort();return{schemaVersion:1,width:config.width,height:config.height,profile:config.profile,intersections,lanes,gateways,features:{arterialRow,arterialColumn,ringEnabled:config.profile==='ring'||config.profile==='mixed'}}}
export function validateTrafficCity(city:TrafficCity):{valid:boolean;reasons:string[]}{const reasons:string[]=[],ids=new Set<string>();for(const item of city.intersections){if(ids.has(item.id))reasons.push(`duplicate intersection ${item.id}`);ids.add(item.id)}const laneIds=new Set<string>();for(const lane of city.lanes){if(laneIds.has(lane.id))reasons.push(`duplicate lane ${lane.id}`);laneIds.add(lane.id);if(!ids.has(lane.from)||!ids.has(lane.to))reasons.push(`orphan lane ${lane.id}`);if(!city.lanes.some(reverse=>reverse.from===lane.to&&reverse.to===lane.from))reasons.push(`missing reverse ${lane.id}`);if(lane.length<1)reasons.push(`invalid length ${lane.id}`)}if(city.intersections.length){const seen=new Set<string>([city.intersections[0].id]),queue=[city.intersections[0].id];while(queue.length){const id=queue.shift()!;for(const lane of city.lanes)if(lane.from===id&&!seen.has(lane.to)){seen.add(lane.to);queue.push(lane.to)}}if(seen.size!==city.intersections.length)reasons.push('disconnected network')}if(city.gateways.length<4)reasons.push('insufficient gateways');for(const gateway of city.gateways)if(!ids.has(gateway))reasons.push(`missing gateway ${gateway}`);return{valid:reasons.length===0,reasons}}
