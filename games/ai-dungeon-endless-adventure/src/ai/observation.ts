import type{DungeonEnemy,DungeonState}from '../state/types';
import{visibleDungeonCells}from '../spatial/visibility';
export interface DungeonObservation{tick:number;hero:{cell:number;hp:number;maxHp:number;energy:number;gold:number;potions:number;shield:number};visibleCells:{cell:number;tile:0|1}[];visibleEnemies:DungeonEnemy[];known:{sigil:boolean;gate:boolean;chest:boolean;shrine:boolean};floor:number}
export function refreshedDungeonKnowledge(state:DungeonState){const seen=new Set(state.ai.knownCells);for(const cell of visibleDungeonCells(state.floor.tiles,state.floor.width,state.floor.height,state.hero.cell,state.hero.vision))seen.add(cell);return[...seen].sort((a,b)=>a-b)}
export function buildDungeonObservation(state:DungeonState):DungeonObservation{const visible=visibleDungeonCells(state.floor.tiles,state.floor.width,state.floor.height,state.hero.cell,state.hero.vision),visibleSet=new Set(visible),visibleCells=visible.map(cell=>({cell,tile:state.floor.tiles[cell]}));const visibleEnemies=state.enemies.filter(enemy=>enemy.alive&&visibleSet.has(enemy.cell)).map(enemy=>({...enemy}));const remembered=new Set(state.ai.knownCells);for(const item of visibleCells)remembered.add(item.cell);return{tick:state.tick,hero:{cell:state.hero.cell,hp:state.hero.hp,maxHp:state.hero.maxHp,energy:state.hero.energy,gold:state.hero.gold,potions:state.hero.potions,shield:state.hero.shield},visibleCells,visibleEnemies,known:{sigil:remembered.has(state.floor.sigil)||state.floorProgress.sigilCollected,gate:remembered.has(state.floor.gate),chest:remembered.has(state.floor.chest)||state.floorProgress.chestOpened,shrine:remembered.has(state.floor.shrine)||state.floorProgress.shrineUsed},floor:state.floorNumber}}
