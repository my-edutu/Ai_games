export interface DungeonRect{x:number;y:number;width:number;height:number}
export interface DungeonLayout{width:number;height:number;compact:boolean;top:DungeonRect;stage:DungeonRect;side:DungeonRect;caption:DungeonRect}
const clamp=(value:number,min:number,max:number)=>Math.max(min,Math.min(max,value));
export function computeDungeonLayout(width:number,height:number,cleanFeed=false):DungeonLayout{const w=clamp(Math.floor(width),320,7680),h=clamp(Math.floor(height),180,4320),margin=clamp(Math.floor(Math.min(w,h)*0.012),4,16),gap=clamp(Math.floor(Math.min(w,h)*0.008),3,10),topHeight=cleanFeed?0:clamp(Math.floor(h*0.065),34,64),captionHeight=cleanFeed?0:clamp(Math.floor(h*0.065),40,58),compact=w<1040||h<560,sideWidth=0,stageY=margin+topHeight+(topHeight?gap:0),stageHeight=Math.max(110,h-stageY-captionHeight-(captionHeight?gap:0)-margin),stageWidth=Math.max(200,w-margin*2),stage={x:margin,y:stageY,width:stageWidth,height:stageHeight},side={x:stage.x+stage.width,y:stageY,width:sideWidth,height:stageHeight},caption={x:margin,y:stage.y+stage.height+(captionHeight?gap:0),width:w-margin*2,height:captionHeight};return{width:w,height:h,compact,top:{x:margin,y:margin,width:w-margin*2,height:topHeight},stage,side,caption}}
