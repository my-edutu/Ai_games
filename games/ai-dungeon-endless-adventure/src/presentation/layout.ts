export interface DungeonRect{x:number;y:number;width:number;height:number}
export interface DungeonLayout{width:number;height:number;compact:boolean;top:DungeonRect;stage:DungeonRect;side:DungeonRect;caption:DungeonRect}
const clamp=(value:number,min:number,max:number)=>Math.max(min,Math.min(max,value));
export function computeDungeonLayout(width:number,height:number,cleanFeed=false):DungeonLayout{const w=clamp(Math.floor(width),320,7680),h=clamp(Math.floor(height),180,4320),margin=clamp(Math.floor(Math.min(w,h)*0.018),6,24),gap=clamp(Math.floor(Math.min(w,h)*0.012),4,16),topHeight=clamp(Math.floor(h*0.09),38,82),captionHeight=clamp(Math.floor(h*0.12),40,96),compact=w<900||h<500,sideWidth=cleanFeed||compact?0:clamp(Math.floor(w*0.21),260,390),stageY=margin+topHeight+gap,stageHeight=Math.max(96,h-stageY-captionHeight-gap-margin),stageWidth=Math.max(160,w-margin*2-(sideWidth?sideWidth+gap:0)),stage={x:margin,y:stageY,width:stageWidth,height:stageHeight},side={x:stage.x+stage.width+(sideWidth?gap:0),y:stageY,width:sideWidth,height:stageHeight},caption={x:margin,y:stage.y+stage.height+gap,width:w-margin*2,height:captionHeight};return{width:w,height:h,compact,top:{x:margin,y:margin,width:w-margin*2,height:topHeight},stage,side,caption}}
