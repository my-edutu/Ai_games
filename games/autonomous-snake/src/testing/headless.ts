import{checksum}from '../../../../packages/replay/src/index';import{SnakeRuntime}from '../runtime/run';
export interface HeadlessOptions{seed:string;runs:number;maxTicks:number;width:number;height:number;targetLength:number}
export function runHeadless(o:HeadlessOptions){const runs=[] as Array<Record<string,unknown>>;let invariantFailures=0,totalTicks=0;for(let i=0;i<o.runs;i++){const seed=`${o.seed}:${i}`,r=SnakeRuntime.create({width:o.width,height:o.height,targetLength:o.targetLength},seed);let ticks=0;while(!r.state.result&&ticks<o.maxTicks){r.step();ticks++;const body=r.state.snake.body;if(new Set(body).size!==body.length){invariantFailures++;break}if(r.state.food!==null&&body.includes(r.state.food)){invariantFailures++;break}}totalTicks+=ticks;runs.push({seed,ticks,outcomeType:r.state.result?'game':'technical',terminal:r.state.result?.reason??'tick-cap',score:r.state.score,length:r.state.snake.body.length,checksum:checksum(r.state)})}return{schemaVersion:1,options:{...o},runs,invariantFailures,totalTicks}}
