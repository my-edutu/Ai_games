import type{Direction,GameLifecycle,SnakeConfig,SnakeRunResult}from '../../../../packages/game-contracts/src/index';
import type{InfluenceRuntimeState}from '../influence/types';
export interface SnakeAiState{mode:string;confidence:number;explanation:string;decisions:number;fallbackCount:number;replans:number;repeatCount:number;recentHashes:string[];nodeExpansions:number}
export type ProgressionBand='small'|'medium'|'major'|'conquest';
export interface SnakeState{schemaVersion:1;runId:string;seed:string;tick:number;movementStep:number;lifecycle:GameLifecycle;config:SnakeConfig;snake:{body:number[];direction:Direction};obstacles:number[];hazards:number[];portalPairs:Record<number,number>;boardFeatures:{profile:string;obstacleRatio:number;hazardCount:number;portalPairs:number;playableCells:number};food:number|null;foodKind:'standard'|'bonus';foodExpiresAt:number|null;foodsCollected:number;score:number;occupancy:number;ai:SnakeAiState;influence:InfluenceRuntimeState;progressionBand:ProgressionBand;lastMilestoneLength:number;result?:SnakeRunResult;intermissionRemaining:number;meaningfulEventTick:number}
export interface SnakeEvent{seq:number;tick:number;type:string;data?:Record<string,unknown>}
