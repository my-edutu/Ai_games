import type{RenderSnapshot,PublicScene}from './snapshot';import{deriveScene}from './scene';
export class PresentationHost{private latest?:RenderSnapshot;private failed=false;private failureReason='';accept(snapshot:RenderSnapshot){if(snapshot.version!==1)return{accepted:false,reason:'unsupported-version'};if(this.latest&&snapshot.tick<this.latest.tick)return{accepted:false,reason:'stale'};this.latest=snapshot;this.failed=false;this.failureReason='';return{accepted:true,reason:'ok'}}fail(reason:string){this.failed=true;this.failureReason=reason}scene():PublicScene{return this.failed?'recovery':this.latest?deriveScene(this.latest):'maintenance'}rebuild(){if(!this.latest)throw new Error('no-snapshot');this.failed=false;this.failureReason='';return this.latest}current(){if(!this.latest)throw new Error('no-snapshot');return this.latest}diagnostic(){return{failed:this.failed,reason:this.failureReason,tick:this.latest?.tick??-1}}}
