import type{Direction,SnakeAction,SnakeRunResult}from '../../../../packages/game-contracts/src/index';import type{NamedRng}from '../../../../packages/seeded-rng/src/index';import{checksum}from '../../../../packages/replay/src/index';import{spawnFood}from '../generation/food';import type{SnakeEvent,SnakeState}from '../state/types';
const delta:Record<Direction,[number,number]>={up:[0,-1],down:[0,1],left:[-1,0],right:[1,0]};const opposite:Record<Direction,Direction>={up:'down',down:'up',left:'right',right:'left'};
function destination(state:SnakeState,d:Direction){const h=state.snake.body[0],x=h%state.config.width,y=Math.floor(h/state.config.width),[dx,dy]=delta[d];const nx=x+dx,ny=y+dy;return{nx,ny,index:ny*state.config.width+nx}}
export function isLegalAction(state:SnakeState,a:SnakeAction){if(state.lifecycle!=='running'||opposite[state.snake.direction]===a.direction)return false;const d=destination(state,a.direction);if(d.nx<0||d.ny<0||d.nx>=state.config.width||d.ny>=state.config.height)return false;const grow=d.index===state.food;const blocked=grow?state.snake.body:state.snake.body.slice(0,-1);return !blocked.includes(d.index)}
export interface StepOutput{state:SnakeState;events:SnakeEvent[]}
export function applyAction(input:SnakeState,a:SnakeAction,rng:NamedRng):StepOutput{const state:SnakeState=JSON.parse(JSON.stringify(input));const events:SnakeEvent[]=[];if(state.lifecycle!=='running')return{state,events};const dir=opposite[state.snake.direction]===a.direction?state.snake.direction:a.direction;const d=destination(state,dir);state.tick++;state.movementStep++;const fail=(reason:'wall-collision'|'self-collision')=>{const result:SnakeRunResult={kind:'game',reason,tick:state.tick,score:state.score,length:state.snake.body.length};state.lifecycle='result';state.result=result;state.intermissionRemaining=state.config.intermissionTicks;state.result.finalChecksum=checksum({...state,result:{...state.result,finalChecksum:undefined}});events.push({seq:0,tick:state.tick,type:'collision',data:{reason}},{seq:1,tick:state.tick,type:'result',data:{reason}})};if(d.nx<0||d.ny<0||d.nx>=state.config.width||d.ny>=state.config.height){fail('wall-collision');return{state,events}}const grow=d.index===state.food;const blocked=grow?state.snake.body:state.snake.body.slice(0,-1);if(blocked.includes(d.index)){fail('self-collision');return{state,events}}state.snake.direction=dir;state.snake.body.unshift(d.index);events.push({seq:0,tick:state.tick,type:'move',data:{direction:dir}});if(grow){state.score++;state.occupancy=state.snake.body.length;state.meaningfulEventTick=state.tick;events.push({seq:1,tick:state.tick,type:'food-collected'},{seq:2,tick:state.tick,type:'growth',data:{length:state.snake.body.length}});if(state.snake.body.length>=state.config.targetLength){state.lifecycle='result';state.result={kind:'game',reason:'victory',tick:state.tick,score:state.score,length:state.snake.body.length};state.intermissionRemaining=state.config.intermissionTicks;events.push({seq:3,tick:state.tick,type:'victory'},{seq:4,tick:state.tick,type:'result',data:{reason:'victory'}})}else{state.food=spawnFood(state,rng);events.push({seq:3,tick:state.tick,type:'food-spawned',data:{cell:state.food}})}}else{state.snake.body.pop();state.occupancy=state.snake.body.length}if(state.result)state.result.finalChecksum=checksum({...state,result:{...state.result,finalChecksum:undefined}});return{state,events}}
export const DIRECTIONS:Direction[]=['up','right','down','left'];
