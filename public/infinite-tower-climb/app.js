'use strict';
(()=>{
const canvas=document.getElementById('tower-canvas'),ctx=canvas.getContext('2d',{alpha:false}),body=document.body,params=new URLSearchParams(location.search);
const reduced=params.get('reducedMotion')==='1',contrast=params.get('highContrast')==='1',muted=params.get('muted')==='1',clean=params.get('cleanFeed')==='1';
body.dataset.reducedMotion=String(reduced);body.dataset.highContrast=String(contrast);body.classList.toggle('clean-feed',clean);
const $=id=>document.getElementById(id),q=id=>document.querySelector(`[data-testid=${id}]`),MAX_PARTICLES=96;
let current=null,polling=false,closed=false,lastChecksum='',visual=null,lastState=null,lastFloor=-1,lastAudioId='',audioCtx=null,master=null;
const particles=[];
const PALETTES={
foundry:{sky0:'#120b0b',sky1:'#351711',fog:'#ff7b32',edge:'#ffb45f',metal:'#3e2f2d',metal2:'#17191d',accent:'#ff5f2e',ambient:'#ff8a4a'},
ruins:{sky0:'#11171b',sky1:'#2b3530',fog:'#b4c89b',edge:'#d8c59e',metal:'#4a4940',metal2:'#1b211d',accent:'#9bc577',ambient:'#c1d9a5'},
storm:{sky0:'#07101e',sky1:'#172b45',fog:'#7ad9ff',edge:'#9eeaff',metal:'#29394d',metal2:'#111a28',accent:'#54c8ff',ambient:'#6de2ff'},
clockwork:{sky0:'#120d18',sky1:'#39263e',fog:'#e8b969',edge:'#ffd88e',metal:'#5a4636',metal2:'#1c1720',accent:'#f0b84c',ambient:'#eac16e'},
void:{sky0:'#05050b',sky1:'#161027',fog:'#a069ff',edge:'#d9c4ff',metal:'#29223d',metal2:'#0d0b14',accent:'#9b5dff',ambient:'#bb86ff'}};
function fit(){const d=Math.min(2,devicePixelRatio||1),r=canvas.getBoundingClientRect(),w=Math.max(1,Math.round(r.width*d)),h=Math.max(1,Math.round(r.height*d));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;ctx.setTransform(d,0,0,d,0,0)}}
function sceneName(scene){return({normal:'ASCENDING',danger:'DANGER',upgrade:'BUILD CHOICE',guardian:'GUARDIAN',result:'RUN COMPLETE',intermission:'NEXT RUN',recovery:'RECOVERING'})[scene]||String(scene).toUpperCase()}
function setText(testId,value){const node=q(testId);if(node)node.textContent=String(value)}
function hash(s){let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return(h>>>0)/4294967295}
function lerp(a,b,t){return a+(b-a)*t}
function clamp(v,a,b){return Math.max(a,Math.min(b,v))}
function palette(theme){return PALETTES[theme]||PALETTES.foundry}
function polygon(points,fill,stroke,line=1){ctx.beginPath();ctx.moveTo(points[0][0],points[0][1]);for(let i=1;i<points.length;i++)ctx.lineTo(points[i][0],points[i][1]);ctx.closePath();if(fill){ctx.fillStyle=fill;ctx.fill()}if(stroke){ctx.strokeStyle=stroke;ctx.lineWidth=line;ctx.stroke()}}
function line(x1,y1,x2,y2,color,width=1,alpha=1){ctx.globalAlpha=alpha;ctx.strokeStyle=color;ctx.lineWidth=width;ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke();ctx.globalAlpha=1}
function glow(x,y,r,color,a=.3){const g=ctx.createRadialGradient(x,y,0,x,y,r);g.addColorStop(0,color);g.addColorStop(1,'transparent');ctx.globalAlpha=a;ctx.fillStyle=g;ctx.fillRect(x-r,y-r,r*2,r*2);ctx.globalAlpha=1}
function update(data){const s=data.snapshot;if(!s)return;window.__TOWER_PUBLIC_STATE__=s;setText('floor',s.floor);setText('height',`${Math.floor(s.progress.height/1000)}m`);setText('guardian',s.progress.nextGuardianFloor);setText('tick',s.tick);setText('health',`${s.player.health}/${s.player.maxHealth}`);setText('stamina',s.player.stamina);setText('shields',s.player.shieldCharges);setText('ai-intent',s.intent.summary.toUpperCase());setText('score',s.player.score.toLocaleString());setText('scene',sceneName(data.scene));setText('run-token',s.runToken.slice(0,8));const progress=Math.round(s.progress.floorProgressPermille/10);setText('progress',`${progress}%`);$('floor-progress').style.width=`${progress}%`;$('health-bar').style.width=`${100*s.player.health/s.player.maxHealth}%`;$('stamina-bar').style.width=`${100*s.player.stamina/s.player.maxStamina}%`;$('theme-name').textContent=`${s.theme.toUpperCase()} SECTOR`;$('intent-mode').textContent=s.intent.mode.toUpperCase();$('confidence').textContent=`${Math.round(s.intent.confidencePermille/10)}%`;$('caption-text').textContent=data.audio?.captions?.[0]?.text||s.intent.summary;body.classList.toggle('danger',data.scene==='danger'||data.scene==='guardian');body.dataset.theme=s.theme;const tags=$('build-tags');tags.replaceChildren(...(s.buildTags.length?s.buildTags:['BASE KIT']).map(text=>{const e=document.createElement('em');e.textContent=text.replaceAll('-',' ').toUpperCase();return e}));$('choice-card').hidden=data.scene!=='upgrade';$('result-card').hidden=!['result','intermission'].includes(data.scene);$('recovery-card').hidden=data.scene!=='recovery';if(!visual||lastState?.runToken!==s.runToken)visual={x:s.player.x,y:s.player.y,vx:s.player.vx,vy:s.player.vy};if(lastFloor>=0&&s.floor!==lastFloor)burst(canvas.clientWidth*.5,canvas.clientHeight*.35,palette(s.theme).edge,30,'milestone');lastFloor=s.floor;lastState=s;playFrameAudio(data.audio)}
function transform(s,w,h,camera){const base=Math.min(w/(s.worldWidth/1000+105),h/(s.chunkHeight/1000*.68)),scale=base/Math.max(.78,camera?.zoom||1),cx=(camera?.centerX||s.worldWidth/2)/1000,cy=((camera?.centerY||s.player.y)-s.chunkBaseY)/1000;const shake=!reduced&&(camera?.impulse||0)>.05?(hash(`${s.tick}:x`)-.5)*8*(camera.impulse||0):0;return{x:v=>w/2+(v/1000-cx)*scale+shake,y:v=>h*.63-(v/1000-cy)*scale,scale,depth:(x,z)=>x+z*scale}}
function drawSky(s,w,h,p,time){const g=ctx.createLinearGradient(0,0,0,h);g.addColorStop(0,p.sky0);g.addColorStop(.62,p.sky1);g.addColorStop(1,'#030407');ctx.fillStyle=g;ctx.fillRect(0,0,w,h);const moonX=w*.76,moonY=h*.18;glow(moonX,moonY,h*.23,p.fog,.18);ctx.globalAlpha=.22;ctx.fillStyle=p.edge;ctx.beginPath();ctx.arc(moonX,moonY,h*.06,0,Math.PI*2);ctx.fill();ctx.globalAlpha=1;for(let layer=0;layer<4;layer++){const y=h*(.22+layer*.14),speed=reduced?0:time*(.004+layer*.002);ctx.globalAlpha=.05+layer*.04;ctx.fillStyle=layer<2?p.fog:p.metal;ctx.beginPath();ctx.moveTo(0,h);for(let x=0;x<=w+120;x+=80){const n=hash(`${s.theme}:${layer}:${Math.floor((x+speed)%2000)}`),peak=y+n*h*.12;ctx.lineTo(x,peak)}ctx.lineTo(w,h);ctx.closePath();ctx.fill()}ctx.globalAlpha=1;if(s.theme==='storm'){for(let i=0;i<18;i++){const x=(hash(`rainx${i}`)*w+time*.15*(i%3+1))%w,y=(hash(`rainy${i}`)*h+time*.32*(i%5+2))%h;line(x,y,x-13,y+30,'#9eeaff',1,.17)}}else if(s.theme==='void'){for(let i=0;i<28;i++){const x=hash(`starx${i}`)*w,y=hash(`stary${i}`)*h*.72,r=1+hash(`starr${i}`)*1.7;ctx.globalAlpha=.25+.45*hash(`stara${i}`);ctx.fillStyle='#e6d9ff';ctx.fillRect(x,y,r,r)}ctx.globalAlpha=1}}
function drawTowerDepth(s,w,h,p,time){const vanX=w*.5,tiers=[.08,.16,.27,.4];for(let i=0;i<tiers.length;i++){const inset=w*tiers[i],top=h*(.02+i*.06),alpha=.14+i*.06;ctx.globalAlpha=alpha;polygon([[inset,h],[w-inset,h],[w*.61,top],[w*.39,top]],p.metal2,p.edge,1);for(let y=top+25;y<h;y+=42+i*8)line(lerp(w*.39,inset,(y-top)/(h-top)),y,lerp(w*.61,w-inset,(y-top)/(h-top)),y,p.edge,1,.12)}ctx.globalAlpha=1;for(let i=0;i<8;i++){const side=i%2?1:-1,baseX=vanX+side*(w*.18+(i%4)*w*.055),par=(time*.006*(i%3+1))%80;line(baseX,0,baseX+side*90,h,p.edge,2,.09);for(let y=-80+par;y<h;y+=90)glow(baseX+side*y*.08,y,8,p.accent,.12)}}
function drawPlatform(t,pf,p){const x=t.x(pf.x),y=t.y(pf.y+pf.height),ww=pf.width/1000*t.scale,hh=Math.max(5,pf.height/1000*t.scale),depth=Math.max(8,16*t.scale),slant=depth*.7;const top=pf.kind==='moving'?p.accent:p.edge,front=pf.kind==='moving'?p.metal:p.metal2;polygon([[x,y],[x+ww,y],[x+ww-slant,y-depth],[x+slant,y-depth]],top,null);polygon([[x,y],[x+ww,y],[x+ww,y+hh],[x,y+hh]],front,p.edge,.7);polygon([[x+ww,y],[x+ww-slant,y-depth],[x+ww-slant,y-depth+hh],[x+ww,y+hh]],'#090a0d',null);ctx.globalAlpha=.26;for(let gx=x+18;gx<x+ww-10;gx+=34)line(gx,y+2,gx-8,y-depth+2,p.edge,1,.55);ctx.globalAlpha=1;if(pf.kind==='moving'){glow(x+ww*.5,y-depth*.5,Math.min(55,ww*.35),p.accent,.18);for(let i=0;i<3;i++)glow(x+ww*(.25+i*.25),y+hh*.45,6,p.edge,.65)}}
function drawHazard(t,hz,p,time){const x=t.x(hz.x),y=t.y(hz.y+hz.height),ww=hz.width/1000*t.scale,hh=Math.max(8,hz.height/1000*t.scale),active=hz.active,hot=active?p.accent:'#50515b';ctx.save();ctx.globalAlpha=active?1:.38;if(hz.kind==='spikes'){const n=Math.max(3,Math.floor(ww/12));for(let i=0;i<n;i++)polygon([[x+i*ww/n,y],[x+(i+.5)*ww/n,y-hh*1.7],[x+(i+1)*ww/n,y]],hot,'#1a1110',.7)}else if(hz.kind==='heat'){const g=ctx.createLinearGradient(x,y-hh*2,x,y);g.addColorStop(0,'rgba(255,188,84,0)');g.addColorStop(1,hot);ctx.fillStyle=g;ctx.fillRect(x,y-hh*2,ww,hh*2);for(let i=0;i<5;i++){const px=x+ww*hash(`${hz.id}${i}`),py=y-hh*(1+Math.sin(time*.01+i)*.6);glow(px,py,8,hot,.45)}}else if(hz.kind==='crusher'){ctx.fillStyle=p.metal;ctx.fillRect(x,y-hh*.4,ww,hh*.4);for(let i=0;i<6;i++){const tx=x+i*ww/5;line(tx,y-hh*.4,tx,y-hh*2.2,p.edge,2,.6)}polygon([[x,y-hh*.4],[x+ww,y-hh*.4],[x+ww*.86,y-hh*1.1],[x+ww*.14,y-hh*1.1]],hot,p.edge,1)}else if(hz.kind==='lightning'){line(x+ww*.1,y-hh,x+ww*.5,y-hh*3,hot,4,.7);line(x+ww*.5,y-hh*3,x+ww*.72,y-hh*1.8,'#fff',2,.8);line(x+ww*.72,y-hh*1.8,x+ww*.9,y-hh,hot,4,.7);glow(x+ww*.5,y-hh*2.1,30,hot,.35)}else{ctx.strokeStyle=hot;ctx.lineWidth=3;ctx.beginPath();ctx.ellipse(x+ww*.5,y-hh*.7,ww*.48,hh*.95,0,0,Math.PI*2);ctx.stroke();glow(x+ww*.5,y-hh*.7,ww*.55,hot,.24)}ctx.restore()}
function drawPickup(t,item,p,time){const x=t.x(item.x),y=t.y(item.y),r=6+3*Math.sin(time*.008+hash(item.id)*6);glow(x,y,22,item.kind==='health'?'#6ef5ae':item.kind==='stamina'?'#5fe9ff':p.accent,.28);ctx.save();ctx.translate(x,y);ctx.rotate(time*.001+hash(item.id)*5);const c=item.kind==='health'?'#79ffc0':item.kind==='stamina'?'#7deeff':p.edge;polygon([[0,-r],[r*.8,0],[0,r],[-r*.8,0]],c,'#fff',.8);ctx.restore()}
function drawProjectile(t,item,p){const x=t.x(item.x),y=t.y(item.y),c=item.owner==='player'?'#69eaff':'#ff5d72';glow(x,y,18,c,.5);line(x-item.vx/2600,y+item.vy/2600,x,y,c,3,.7);ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(x,y,2.6,0,Math.PI*2);ctx.fill()}
function drawEnemy(t,e,p,time){if(!e.active)return;const x=t.x(e.x),y=t.y(e.y),rw=Math.max(8,e.halfWidth/1000*t.scale),rh=Math.max(11,e.halfHeight/1000*t.scale);ctx.save();ctx.translate(x,y);const bob=reduced?0:Math.sin(time*.006+hash(e.id)*5)*2;ctx.translate(0,bob);if(e.kind==='guardian'){glow(0,0,rh*2.8,p.accent,.28);ctx.fillStyle=p.metal;ctx.beginPath();ctx.moveTo(-rw*1.5,rh*.9);ctx.lineTo(-rw*1.9,-rh*.45);ctx.lineTo(-rw*.75,-rh*1.35);ctx.lineTo(0,-rh*1.65);ctx.lineTo(rw*.75,-rh*1.35);ctx.lineTo(rw*1.9,-rh*.45);ctx.lineTo(rw*1.5,rh*.9);ctx.closePath();ctx.fill();polygon([[-rw*.7,-rh*.4],[0,-rh*.95],[rw*.7,-rh*.4],[rw*.45,rh*.35],[0,rh*.7],[-rw*.45,rh*.35]],p.accent,p.edge,1.5);for(const side of[-1,1]){line(side*rw*.7,-rh*.2,side*rw*1.9,rh*.45,p.edge,5,.9);glow(side*rw*1.9,rh*.45,10,p.accent,.5)}glow(0,-rh*.55,8,'#fff',.6)}else{const c=e.kind==='shooter'?'#d66bff':'#ff6d7f';glow(0,0,rh*1.7,c,.2);polygon([[-rw*.8,rh],[-rw,-rh*.2],[-rw*.45,-rh],[rw*.45,-rh],[rw,-rh*.2],[rw*.8,rh]],p.metal,p.edge,1);polygon([[-rw*.55,-rh*.2],[0,-rh*.65],[rw*.55,-rh*.2],[rw*.35,rh*.42],[-rw*.35,rh*.42]],c,null);if(e.kind==='shooter'){line(rw*.6,-rh*.1,rw*1.55,-rh*.1,c,4,.9);glow(rw*1.55,-rh*.1,8,c,.45)}}if(e.telegraph){ctx.strokeStyle='#fff';ctx.lineWidth=2;ctx.globalAlpha=.8;ctx.beginPath();ctx.arc(0,0,Math.max(rw,rh)*1.8,0,Math.PI*2);ctx.stroke();ctx.globalAlpha=1}ctx.restore()}
function drawClimber(t,s,p,time){if(!visual)return;visual.x=lerp(visual.x,s.player.x,reduced?.35:.2);visual.y=lerp(visual.y,s.player.y,reduced?.35:.2);visual.vx=lerp(visual.vx,s.player.vx,.18);visual.vy=lerp(visual.vy,s.player.vy,.18);const x=t.x(visual.x),y=t.y(visual.y),rw=Math.max(8,s.player.halfWidth/1000*t.scale),rh=Math.max(14,s.player.halfHeight/1000*t.scale),speed=Math.abs(visual.vx),air=Math.abs(visual.vy)>1400,state=s.player.state||'';const run=air?0:Math.sin(time*(.012+speed/1200000))*clamp(speed/5000,0,1),f=s.player.facing||1,lean=clamp(visual.vx/16000,-.28,.28);ctx.save();ctx.translate(x,y);ctx.scale(f,1);ctx.rotate(lean*.18);if(s.player.shieldCharges>0){ctx.strokeStyle='#6be9ff';ctx.globalAlpha=.28+.08*Math.sin(time*.01);ctx.lineWidth=2;ctx.beginPath();ctx.ellipse(0,0,rw*2.2,rh*1.8,0,0,Math.PI*2);ctx.stroke();ctx.globalAlpha=1}glow(0,0,rh*2.3,p.ambient,.13);const hipY=rh*.25,shoulderY=-rh*.35,headY=-rh*.85;line(-rw*.18,hipY,-rw*.36+run*rw*.25,rh*1.05,p.edge,Math.max(3,rw*.33),1);line(rw*.18,hipY,rw*.46-run*rw*.25,rh*1.05,p.edge,Math.max(3,rw*.33),1);line(-rw*.25,shoulderY,-rw*.68-run*rw*.3,rh*.2,p.edge,Math.max(3,rw*.25),1);line(rw*.25,shoulderY,rw*.78+run*rw*.3,rh*.08,p.edge,Math.max(3,rw*.25),1);ctx.fillStyle='#161a21';ctx.beginPath();ctx.moveTo(-rw*.7,shoulderY);ctx.quadraticCurveTo(-rw*.55,hipY,rw*.48,hipY*1.55);ctx.lineTo(rw*.56,shoulderY);ctx.closePath();ctx.fill();polygon([[-rw*.52,shoulderY],[-rw*.25,-rh*.6],[rw*.28,-rh*.6],[rw*.58,shoulderY],[rw*.36,hipY],[-rw*.35,hipY]],'#27303b',p.edge,1);polygon([[-rw*.3,-rh*.5],[rw*.28,-rh*.5],[rw*.4,-rh*.06],[-rw*.36,-rh*.02]],p.accent,null);ctx.fillStyle='#d8b28d';ctx.beginPath();ctx.arc(0,headY,rw*.48,0,Math.PI*2);ctx.fill();ctx.fillStyle='#11161d';ctx.beginPath();ctx.arc(-rw*.06,headY-rw*.12,rw*.52,Math.PI,Math.PI*2);ctx.fill();polygon([[rw*.28,headY-rw*.06],[rw*.68,headY+rw*.12],[rw*.28,headY+rw*.28]],p.edge,null);if(air||state.includes('jump')){ctx.rotate(-.04);line(-rw*.2,hipY,-rw*.7,rh*.72,p.edge,Math.max(3,rw*.33),1);line(rw*.15,hipY,rw*.75,rh*.45,p.edge,Math.max(3,rw*.33),1)}ctx.restore()}
function routeCue(t,s,p){if(!s.platforms.length)return;const above=s.platforms.filter(v=>v.y>s.player.y+8000).sort((a,b)=>a.y-b.y)[0];if(!above)return;const sx=t.x(s.player.x),sy=t.y(s.player.y),tx=t.x(above.x+above.width/2),ty=t.y(above.y+above.height);ctx.save();ctx.setLineDash([5,9]);ctx.strokeStyle=p.edge;ctx.globalAlpha=.16+.16*(s.intent.confidencePermille/1000);ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(sx,sy);ctx.quadraticCurveTo((sx+tx)/2,Math.min(sy,ty)-45,tx,ty);ctx.stroke();ctx.setLineDash([]);ctx.globalAlpha=1;ctx.restore()}
function burst(x,y,color,count=12,kind='dust'){while(particles.length+count>MAX_PARTICLES)particles.shift();for(let i=0;i<count;i++){const a=Math.PI*2*hash(`${performance.now()}:${i}:${kind}`),sp=18+hash(`sp${i}${kind}`)*70;particles.push({x,y,vx:Math.cos(a)*sp,vy:Math.sin(a)*sp-(kind==='milestone'?55:10),life:1,size:1.5+hash(`sz${i}`)*4,color})}}
function updateParticles(dt){for(let i=particles.length-1;i>=0;i--){const p=particles[i];p.life-=dt*.7;p.x+=p.vx*dt;p.y+=p.vy*dt;p.vy+=44*dt;if(p.life<=0){particles.splice(i,1);continue}ctx.globalAlpha=p.life*.65;ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(p.x,p.y,p.size*p.life,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1}
function drawForeground(s,w,h,p,time){ctx.globalAlpha=.48;for(let i=0;i<5;i++){const side=i%2?1:-1,x=side<0?w*(.02+i*.025):w*(.98-i*.025),swing=reduced?0:Math.sin(time*.0015+i)*18;ctx.strokeStyle='#090a0d';ctx.lineWidth=10-i;ctx.beginPath();ctx.moveTo(x,-20);ctx.bezierCurveTo(x+swing,h*.3,x-swing,h*.65,x+swing,h+30);ctx.stroke();ctx.strokeStyle=p.edge;ctx.globalAlpha=.08;ctx.lineWidth=1;ctx.stroke()}ctx.globalAlpha=1;const vg=ctx.createRadialGradient(w*.5,h*.48,h*.15,w*.5,h*.48,h*.78);vg.addColorStop(.5,'transparent');vg.addColorStop(1,'rgba(0,0,0,.58)');ctx.fillStyle=vg;ctx.fillRect(0,0,w,h)}
function draw(data,time){fit();const w=canvas.clientWidth,h=canvas.clientHeight,s=data?.snapshot,p=palette(s?.theme);drawSky(s||{theme:'foundry'},w,h,p,time);if(!s)return;drawTowerDepth(s,w,h,p,time);const t=transform(s,w,h,data.camera);routeCue(t,s,p);for(const pf of s.platforms)drawPlatform(t,pf,p);for(const hz of s.hazards)drawHazard(t,hz,p,time);for(const item of s.pickups)drawPickup(t,item,p,time);for(const e of s.enemies)drawEnemy(t,e,p,time);for(const projectile of s.projectiles)drawProjectile(t,projectile,p);drawClimber(t,s,p,time);updateParticles(1/60);drawForeground(s,w,h,p,time);if(data.scene==='guardian'){ctx.globalAlpha=.09;ctx.fillStyle=p.accent;ctx.fillRect(0,0,w,h);ctx.globalAlpha=1}if(data.scene==='danger'&&!reduced){const a=.06+.03*Math.sin(time*.02);ctx.fillStyle=`rgba(255,30,65,${a})`;ctx.fillRect(0,0,w,h)}if(s.floor>0&&[10,25,50,100,250,500,1000].includes(s.floor)){ctx.textAlign='center';ctx.fillStyle=p.edge;ctx.font='700 12px system-ui';ctx.globalAlpha=.65;ctx.fillText(`MILESTONE // FLOOR ${s.floor}`,w/2,38);ctx.globalAlpha=1}}
function ensureAudio(){if(muted||audioCtx)return;try{audioCtx=new(window.AudioContext||window.webkitAudioContext)();master=audioCtx.createGain();master.gain.value=.045;master.connect(audioCtx.destination)}catch{audioCtx=null}}
function tone(freq,duration,type='sine',gain=.7,delay=0){if(!audioCtx||audioCtx.state!=='running'||!master)return;const o=audioCtx.createOscillator(),g=audioCtx.createGain(),start=audioCtx.currentTime+delay;o.type=type;o.frequency.setValueAtTime(freq,start);g.gain.setValueAtTime(.001,start);g.gain.exponentialRampToValueAtTime(gain,start+.015);g.gain.exponentialRampToValueAtTime(.001,start+duration);o.connect(g);g.connect(master);o.start(start);o.stop(start+duration+.03)}
function playFrameAudio(audio){if(muted||!audio?.voices?.length)return;ensureAudio();const v=audio.voices[0];if(!v||v.id===lastAudioId)return;lastAudioId=v.id;const map={impact:[90,.18,'sawtooth'],warning:[440,.12,'square'],'floor-rise':[520,.16,'triangle'],'enemy-break':[180,.16,'sawtooth'],upgrade:[660,.2,'triangle'],'fall-end':[70,.45,'sine'],replan:[260,.13,'triangle'],'safe-tone':[220,.3,'sine']},spec=map[v.cue]||[300,.12,'sine'];tone(spec[0],spec[1],spec[2],.45);if(v.cue==='floor-rise'||v.cue==='upgrade')tone(spec[0]*1.5,spec[1],spec[2],.3,.08)}
addEventListener('pointerdown',()=>{ensureAudio();audioCtx?.resume?.()},{once:true});
async function poll(){if(polling||closed)return;polling=true;try{const response=await fetch(`/tower/state?w=${innerWidth}&h=${innerHeight}&reducedMotion=${reduced?1:0}&cleanFeed=${clean?1:0}&muted=${muted?1:0}`,{cache:'no-store'});if(!response.ok)throw new Error(String(response.status));const data=await response.json();current=data;if(data.snapshot?.publicChecksum!==lastChecksum){lastChecksum=data.snapshot?.publicChecksum||'';update(data)}$('provider-state').textContent='AUTONOMOUS CORE ONLINE'}catch(error){$('provider-state').textContent='VIEW RECOVERING — AI CONTINUES';console.warn('tower state unavailable',String(error))}finally{polling=false;if(!closed)setTimeout(poll,110)}}
let lastFrame=performance.now();function render(now){const dt=Math.min(.05,(now-lastFrame)/1000);lastFrame=now;void dt;draw(current,now);requestAnimationFrame(render)}addEventListener('resize',fit);addEventListener('beforeunload',()=>closed=true);fit();poll();requestAnimationFrame(render);
})();
