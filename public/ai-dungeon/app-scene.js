const WORLD_PALETTES={
'ancient-crypt':{floor:'#24283a',floor2:'#30354a',wall:'#34364a',edge:'#151725',accent:'#c9a66b',fog:'rgba(48,54,75,.28)'},
'infernal-forge':{floor:'#2a2022',floor2:'#3b2825',wall:'#47302d',edge:'#180f12',accent:'#ff8f4f',fog:'rgba(91,36,28,.25)'},
'fungal-caverns':{floor:'#1d2b29',floor2:'#273b35',wall:'#32443d',edge:'#0d1716',accent:'#8be7c5',fog:'rgba(52,96,83,.24)'},
'flooded-ruins':{floor:'#1c2936',floor2:'#25394b',wall:'#344b5a',edge:'#0c151d',accent:'#7fc8e8',fog:'rgba(39,78,106,.25)'},
'frozen-vault':{floor:'#26313d',floor2:'#334453',wall:'#435668',edge:'#111a24',accent:'#a8e5ff',fog:'rgba(148,201,225,.18)'},
'arcane-library':{floor:'#262238',floor2:'#34304b',wall:'#443e59',edge:'#12101d',accent:'#bd9cff',fog:'rgba(91,67,130,.25)'},
'void-depths':{floor:'#171522',floor2:'#211d31',wall:'#2f2942',edge:'#090810',accent:'#d47cff',fog:'rgba(45,22,75,.34)'}};
function worldPalette(snapshot){return WORLD_PALETTES[snapshot.world?.biome]||WORLD_PALETTES['ancient-crypt']}
function diamond(cx,cy,w,h){ctx.beginPath();ctx.moveTo(cx,cy-h/2);ctx.lineTo(cx+w/2,cy);ctx.lineTo(cx,cy+h/2);ctx.lineTo(cx-w/2,cy);ctx.closePath()}
function isoScreen(tile,hero,tileW,tileH,cx,cy){return{x:cx+(tile.isoX-hero.isoX)*tileW*.5,y:cy+(tile.isoY-hero.isoY)*tileH}}
function drawIsoTile(tile,p,tw,th,colors){const lift=tile.elevation*th*.62,topY=p.y-lift;if(!tile.walkable){const wallH=th*1.5;ctx.fillStyle=colors.edge;ctx.beginPath();ctx.moveTo(p.x-tw/2,topY);ctx.lineTo(p.x,topY+th/2);ctx.lineTo(p.x,topY+th/2+wallH);ctx.lineTo(p.x-tw/2,topY+wallH);ctx.closePath();ctx.fill();ctx.fillStyle=colors.wall;ctx.beginPath();ctx.moveTo(p.x+tw/2,topY);ctx.lineTo(p.x,topY+th/2);ctx.lineTo(p.x,topY+th/2+wallH);ctx.lineTo(p.x+tw/2,topY+wallH);ctx.closePath();ctx.fill();ctx.fillStyle=tile.visible?colors.floor2:colors.floor;diamond(p.x,topY,tw,th);ctx.fill();ctx.strokeStyle='rgba(255,255,255,.035)';ctx.stroke();return}ctx.fillStyle=tile.visible?(tile.room==='boss'?'rgba(91,41,65,.95)':colors.floor2):colors.floor;diamond(p.x,topY,tw,th);ctx.fill();if(tile.edge){ctx.strokeStyle='rgba(210,220,255,.055)';ctx.lineWidth=1;ctx.stroke()}if(tile.elevation){ctx.fillStyle=colors.edge;ctx.beginPath();ctx.moveTo(p.x-tw/2,topY);ctx.lineTo(p.x,topY+th/2);ctx.lineTo(p.x,topY+th/2+th*.35);ctx.lineTo(p.x-tw/2,topY+th*.35);ctx.closePath();ctx.fill()}}
function drawWorldProp(prop,p,tw,th,colors,time,reduced){const s=Math.max(4,tw*.11),bob=reduced?0:Math.sin(time*.002+prop.cell)*1.5;ctx.save();ctx.translate(p.x,p.y-th*.2+bob);ctx.strokeStyle='rgba(10,10,18,.7)';ctx.lineWidth=Math.max(1,s*.12);if(prop.kind==='pillar'||prop.kind==='statue'||prop.kind==='ice-spire'){ctx.fillStyle=prop.kind==='ice-spire'?'rgba(167,224,245,.7)':colors.wall;ctx.beginPath();ctx.moveTo(-s,0);ctx.lineTo(0,s*.42);ctx.lineTo(s,0);ctx.lineTo(s,-s*2.4);ctx.lineTo(0,-s*1.95);ctx.lineTo(-s,-s*2.4);ctx.closePath();ctx.fill();ctx.stroke()}else if(prop.kind==='torch'||prop.kind==='crystal'||prop.kind==='mushroom'){ctx.strokeStyle=colors.wall;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(0,-s*1.3);ctx.stroke();ctx.shadowColor=colors.accent;ctx.shadowBlur=s*2;ctx.fillStyle=colors.accent;ctx.beginPath();ctx.arc(0,-s*1.55,prop.kind==='mushroom'?s*.55:s*.32,0,Math.PI*2);ctx.fill();ctx.shadowBlur=0}else if(prop.kind==='bookshelf'){ctx.fillStyle='#342b29';ctx.fillRect(-s*1.15,-s*2.1,s*2.3,s*2.1);ctx.strokeStyle=colors.accent;for(let i=0;i<3;i++){ctx.beginPath();ctx.moveTo(-s,s*(-.55-i*.5));ctx.lineTo(s,s*(-.55-i*.5));ctx.stroke()}}else if(prop.kind==='altar'){ctx.fillStyle=colors.wall;diamond(0,-s*.15,s*2.5,s);ctx.fill();ctx.fillRect(-s*.72,-s*.2,s*1.44,s*1.25)}else{ctx.fillStyle=colors.wall;for(let i=0;i<3;i++){ctx.beginPath();ctx.arc((i-1)*s*.45,-Math.abs(i-1)*s*.12,s*(.38+i*.08),0,Math.PI*2);ctx.fill()}}ctx.restore()}
function drawInteractable(item,p,tw,th,colors,time,reduced){const s=Math.max(10,tw*.35);ctx.save();ctx.translate(p.x,p.y-th*.34);ctx.shadowColor=colors.accent;ctx.shadowBlur=item.state==='active'?s*.7:s*.2;objectiveGlyph(item.kind,0,0,s,time,reduced);ctx.shadowBlur=0;if(item.state==='sealed'){ctx.strokeStyle=palette.danger;ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,s*.42,0,Math.PI*2);ctx.stroke()}ctx.restore()}
function drawWorldAtmosphere(stage,snapshot,colors,time,reduced){const g=ctx.createRadialGradient(stage.x+stage.width*.5,stage.y+stage.height*.47,stage.height*.04,stage.x+stage.width*.5,stage.y+stage.height*.5,stage.width*.7);g.addColorStop(0,colors.fog);g.addColorStop(1,'rgba(4,5,10,.94)');ctx.fillStyle=g;ctx.fillRect(stage.x,stage.y,stage.width,stage.height);if(!reduced){ctx.globalAlpha=.13;ctx.fillStyle=colors.accent;for(let i=0;i<18;i++){const x=stage.x+((i*173+time*.008*(i%3+1))%stage.width),y=stage.y+((i*97+Math.sin(time*.0006+i)*35)%stage.height);ctx.beginPath();ctx.arc(x,y,1+(i%3),0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1}}
function drawMap(snapshot,stage,time,reduced){const world=snapshot.world;if(!world||!world.tiles?.length)return;const colors=worldPalette(snapshot);ctx.save();roundedRect(stage.x,stage.y,stage.width,stage.height,12);ctx.clip();ctx.translate(reduced?0:shake.x,reduced?0:shake.y);drawWorldAtmosphere(stage,snapshot,colors,time,reduced);const tw=clamp(Math.min(stage.width/14,stage.height/7.4),38,92),th=tw*.54,heroWorld={isoX:snapshot.hero.worldX-snapshot.hero.worldY,isoY:(snapshot.hero.worldX+snapshot.hero.worldY)*.5-snapshot.hero.elevation};const cx=stage.x+stage.width*.5,cy=stage.y+stage.height*.5+th*.7;const projected=new Map();for(const tile of world.tiles){const p=isoScreen(tile,heroWorld,tw,th,cx,cy);projected.set(tile.cell,p)}const ordered=[...world.tiles].sort((a,b)=>(a.isoY+a.elevation*.2)-(b.isoY+b.elevation*.2)||a.isoX-b.isoX);for(const tile of ordered){const p=projected.get(tile.cell);if(p.x<stage.x-tw||p.x>stage.x+stage.width+tw||p.y<stage.y-th*3||p.y>stage.y+stage.height+th*3)continue;drawIsoTile(tile,p,tw,th,colors)}const sceneItems=[];for(const prop of world.props){const tile=world.tiles.find(t=>t.cell===prop.cell);if(tile)sceneItems.push({depth:tile.isoY+.2,type:'prop',value:prop})}for(const item of world.interactables){const tile=world.tiles.find(t=>t.cell===item.cell);if(tile)sceneItems.push({depth:tile.isoY+.35,type:'interactable',value:item})}for(const entity of snapshot.entities){const tile=world.tiles.find(t=>t.cell===entity.cell);if(tile)sceneItems.push({depth:tile.isoY+.5,type:'enemy',value:entity})}const heroTile=world.tiles.find(t=>t.cell===snapshot.hero.cell);if(heroTile)sceneItems.push({depth:heroTile.isoY+.55,type:'hero',value:snapshot.hero});sceneItems.sort((a,b)=>a.depth-b.depth);for(const item of sceneItems){const p=projected.get(item.value.cell);if(!p)continue;if(item.type==='prop')drawWorldProp(item.value,p,tw,th,colors,time,reduced);else if(item.type==='interactable')drawInteractable(item.value,p,tw,th,colors,time,reduced);else if(item.type==='enemy'){const e=item.value,scale=e.kind==='chapter-boss'?tw*1.15:tw*.72;ctx.save();if(e.telegraph){ctx.strokeStyle=palette.danger;ctx.globalAlpha=.5+.25*Math.sin(time*.014);ctx.lineWidth=Math.max(2,tw*.035);ctx.beginPath();ctx.ellipse(p.x,p.y+th*.04,tw*.42,th*.28,0,0,Math.PI*2);ctx.stroke();ctx.globalAlpha=1}if(e.animation==='leap'&&!reduced)ctx.translate(0,-Math.abs(Math.sin(time*.012))*th*.7);drawEnemy(e,p.x,p.y-th*.45,scale,time,reduced);bar(p.x-tw*.28,p.y-th*.96,tw*.56,Math.max(3,tw*.035),e.hp/e.maxHp,e.kind==='chapter-boss'?palette.danger:palette.warning);ctx.restore()}else{ctx.save();if(item.value.animation==='advance'&&!reduced)ctx.translate(0,Math.sin(time*.012)*2);drawHero(p.x,p.y-th*.54,tw*.82,item.value.facing,time,reduced);ctx.restore()}}for(const light of world.lights){const p=projected.get(light.cell);if(!p)continue;const r=tw*(light.radius*.18);const glow=ctx.createRadialGradient(p.x,p.y-th*.45,0,p.x,p.y-th*.45,r);glow.addColorStop(0,`${colors.accent}55`);glow.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=glow;ctx.fillRect(p.x-r,p.y-th*.45-r,r*2,r*2)}if(world.director.mode==='boss'){ctx.strokeStyle='rgba(255,114,133,.24)';ctx.lineWidth=2;roundedRect(stage.x+6,stage.y+6,stage.width-12,stage.height-12,10);ctx.stroke()}ctx.restore();text(world.biome.replace(/-/g,' ').toUpperCase(),stage.x+18,stage.y+22,10,'900','left',colors.accent);if(snapshot.boss)text(`BOSS · PHASE ${snapshot.boss.phase}`,stage.x+stage.width/2,stage.y+26,14,'900','center',palette.danger)}
function drawTop(snapshot,rect){if(rect.height<=0)return;const pad=12;roundedRect(rect.x,rect.y,rect.width,rect.height,12);ctx.fillStyle='rgba(8,10,18,.76)';ctx.fill();ctx.strokeStyle='rgba(173,164,255,.12)';ctx.stroke();text(`FLOOR ${snapshot.progress.floor}`,rect.x+pad,rect.y+rect.height*.5,clamp(rect.height*.34,14,22),'900','left',palette.text);text(snapshot.objective.label,rect.x+rect.width*.19,rect.y+rect.height*.5,clamp(rect.height*.26,11,18),'800','left',palette.text,rect.width*.42);const healthW=Math.min(260,rect.width*.2),healthX=rect.x+rect.width-healthW-pad;bar(healthX,rect.y+rect.height*.27,healthW,clamp(rect.height*.17,7,11),snapshot.hero.hp/snapshot.hero.maxHp,snapshot.hero.hp/snapshot.hero.maxHp<.35?palette.danger:palette.safe);bar(healthX,rect.y+rect.height*.62,healthW,clamp(rect.height*.11,5,8),snapshot.hero.energy/snapshot.hero.maxEnergy,palette.violet)}
function drawSide(snapshot,rect){return}
function drawCompact(snapshot,stage){const w=Math.min(stage.width*.68,620),h=42,x=stage.x+(stage.width-w)/2,y=stage.y+stage.height-h-12;roundedRect(x,y,w,h,12);ctx.fillStyle='rgba(7,9,17,.82)';ctx.fill();ctx.strokeStyle=palette.panelEdge;ctx.stroke();text(snapshot.intent.action,x+14,y+14,11,'800','left',palette.text,w-28);text(`${snapshot.intent.confidenceBand} · ${snapshot.danger.label} · ${snapshot.hero.gold} gold`,x+14,y+30,9,'700','left',snapshot.danger.band==='high'||snapshot.danger.band==='critical'?palette.danger:palette.muted,w-28)}
function drawEffects(){for(const p of particles){ctx.globalAlpha=clamp(p.life/50,0,1);ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(p.x,p.y,p.size,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1;for(const f of floaters){ctx.globalAlpha=clamp(f.life/45,0,1);text(f.text,f.x,f.y,14,'800','center',f.color,Math.min(innerWidth*.7,900))}ctx.globalAlpha=1}
