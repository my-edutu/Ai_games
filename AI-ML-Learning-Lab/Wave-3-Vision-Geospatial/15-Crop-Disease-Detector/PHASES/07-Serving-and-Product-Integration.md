# Phase 7 — Serving and Product Integration

Serve image inference through API and/or optimized edge/mobile path with crop, top predictions, confidence and capture-quality feedback.

**Build:** inference API, preprocessing parity tests, model export, latency/device benchmarks.

**Learn:** image serving, ONNX/TFLite concepts, preprocessing consistency.

**Exit:** training and serving preprocess images identically.