# Phase 5 — Model Development and Experimentation

Compare suitable pretrained backbones, augmentation strategies, class balancing and optional segmentation/localization only if evidence requires it.

**Build:** experiment registry, ablations, tuning, calibration, latency/model-size comparison.

**Learn:** CNNs/ViTs, fine-tuning, regularization, augmentation.

**Exit:** chosen model improves field-like holdout performance, not just benchmark images.