# Runbook
Fail closed if no forecast artifact is configured. Watch MAE, interval coverage, missing-day rate and schema failures. Roll back model/data/scenario-policy versions independently. Suspend automated use if interval coverage or error exceeds SLO thresholds.
