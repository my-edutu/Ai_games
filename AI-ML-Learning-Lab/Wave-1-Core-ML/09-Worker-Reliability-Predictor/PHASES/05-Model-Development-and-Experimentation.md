# Phase 5 — Model Development and Experimentation

Compare regularized linear and boosted models with strict temporal validation. Prefer simpler models when gains are marginal.

**Build:** experiment tracking, ablations, calibration, error review.

**Learn:** class weighting, gradient boosting, temporal CV.

**Exit:** selected model improves agreed utility without unacceptable subgroup harm.