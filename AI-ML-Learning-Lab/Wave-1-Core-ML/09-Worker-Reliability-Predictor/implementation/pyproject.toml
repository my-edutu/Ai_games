[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="worker-reliability"
version="0.1.0"
dependencies=["pydantic>=2","numpy","pandas","scikit-learn","fastapi"]
[project.optional-dependencies]
dev=["pytest"]
[tool.pytest.ini_options]
pythonpath=["src"]
