# Model Card
Event-level operational risk-support classifier. It estimates risk for a specific work event; it does not create a persistent worker score. Protected attributes and postcode are excluded from serving features. High-risk outputs are advisory and require human review. The committed benchmark is synthetic/representative, not real-world impact evidence.
