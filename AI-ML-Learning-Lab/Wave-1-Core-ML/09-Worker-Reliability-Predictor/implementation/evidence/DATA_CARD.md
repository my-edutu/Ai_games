# Data Card
Uses event context and pre-event operational history with strict temporal cutoffs. Future history is excluded. Missing history is not treated as negative evidence. Production data requires consent, minimization, provenance, correction workflows and retention limits.
