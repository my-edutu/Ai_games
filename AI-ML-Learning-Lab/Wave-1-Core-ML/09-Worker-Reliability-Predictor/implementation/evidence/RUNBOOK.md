# Runbook
On abnormal risk distribution or calibration drift: switch to advisory-only baseline, inspect input drift and policy changes, verify correction queues, roll back model/data/policy independently, and document the incident. Never bypass human-review requirements during incidents.
