# Phase 7 — Serving and Product Integration

Create endpoints for profile parsing, role parsing, gap analysis, and learning-priority output.

**Build:** API, schema validation, provenance fields, model/version metadata, latency tests.

**Learn:** inference pipelines, caching, API ergonomics.

**Exit:** client can show evidence-backed gaps without exposing opaque internal scores.