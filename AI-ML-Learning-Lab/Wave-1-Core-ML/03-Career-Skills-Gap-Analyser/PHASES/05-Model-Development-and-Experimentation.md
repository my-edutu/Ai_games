# Phase 5 — Model Development and Experimentation

Compare rules, supervised multi-label models, and embedding-based extraction/matching. Add complexity only where baseline error analysis supports it.

**Build:** tracked experiments, embedding/model comparison, confidence calibration, ablation report.

**Learn:** transformers/embeddings, multi-label classification, transfer learning.

**Exit:** chosen approach improves extraction and gap usefulness on held-out roles.