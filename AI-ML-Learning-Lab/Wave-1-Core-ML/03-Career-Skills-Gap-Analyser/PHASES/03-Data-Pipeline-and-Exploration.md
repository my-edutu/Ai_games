# Phase 3 — Data Pipeline and Exploration

Parse resumes/profiles and job descriptions into normalized, quality-checked representations.

**Build:** document ingestion, cleaning, skill normalization, EDA on taxonomy coverage and ambiguous terms, dataset snapshot.

**Learn:** NLP preprocessing, entity extraction, synonym resolution, long-tail distributions.

**Exit:** pipeline produces stable normalized skill records from representative inputs.