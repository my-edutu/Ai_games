# Rollback
Ontology and model versions are registered independently. If alias mapping/taxonomy causes regressions, restore the previous ontology while leaving the active model unchanged. If semantic matching degrades, restore the previous model while retaining the active ontology. Verify release fixtures after rollback and append the action to the audit log.
