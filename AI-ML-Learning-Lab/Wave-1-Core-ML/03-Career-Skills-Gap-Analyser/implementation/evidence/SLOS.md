# SLOs
Controlled-deployment targets: API successful-response rate >= 99%; p95 analysis latency <= 500 ms for typical profiles; unknown evidence must never be converted to absent; unrelated semantic false-positive rate on release fixtures = 0%; every response exposes ontology, role and model versions.
