# Ontology Card
Ontology versions define canonical skill IDs, aliases, categories and prerequisites. Aliases are provenance-controlled and changes are independently versioned from semantic-model changes. Unknown or ambiguous terminology must abstain rather than silently map. Rollback of ontology and model versions is independent.
