# Phase 3 — Data Pipeline and Exploration

Normalize scholarship rules and applicant records; inspect outcome imbalance and missing fields.

**Build:** parsers/normalizers, quality report, EDA, temporal splits, data snapshot.

**Learn:** categorical encoding, imbalance, label noise, temporal leakage.

**Exit:** pipeline rebuilds the training/evaluation set deterministically.