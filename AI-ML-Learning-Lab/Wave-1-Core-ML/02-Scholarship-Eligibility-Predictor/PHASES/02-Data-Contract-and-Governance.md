# Phase 2 — Data Contract and Governance

Model scholarship criteria as structured, versioned rules; define applicant schema and outcome labels separately.

**Build:** criteria schema, applicant schema, source/provenance fields, unknown-value handling, validation tests.

**Learn:** schema evolution, missingness, PII, policy provenance, label bias.

**Exit:** each eligibility decision can point back to source criteria and version.