# Phase 7 — Serving and Product Integration

Expose an API that returns policy eligibility, unmet criteria, unknown criteria, and optional calibrated suitability separately.

**Build:** typed API, policy trace, validation errors, model/version fields, latency tests.

**Learn:** API contracts, validation, serving, versioning.

**Exit:** frontend can render a decision without interpreting raw model internals.