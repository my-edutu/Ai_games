# Phase 8 — MLOps, Monitoring, and Retraining

Track policy changes separately from model changes. Monitor unknown-field rates, criteria parsing failures, calibration drift and prediction distribution.

**Build:** version registry, monitoring dashboards/spec, retraining/recalibration triggers, rollback.

**Learn:** model/version governance, drift, reproducibility.

**Exit:** operators can distinguish policy-data failure from model failure.