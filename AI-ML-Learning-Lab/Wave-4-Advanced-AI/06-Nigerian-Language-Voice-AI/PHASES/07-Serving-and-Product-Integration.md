# Phase 7 — Serving and Product Integration

Build batch/streaming ASR interface with language hints, timestamps, confidence and privacy-safe logging.

**Build:** API/websocket design, audio chunking, model export, latency/load tests, retention controls.

**Learn:** streaming inference, VAD, batching, edge/cloud trade-offs.

**Exit:** service meets measured latency and does not retain audio by accident.