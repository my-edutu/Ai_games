# Phase 5 — Model Development and Experimentation

Fine-tune a suitable pretrained speech model; compare language-balanced sampling, augmentation and decoding/language-model options.

**Build:** tracked runs, ablations, decoding configs, latency/model-size comparison.

**Learn:** transfer learning for ASR, SpecAugment, decoding, language models.

**Exit:** gains hold across speakers/dialects, not only aggregate WER.