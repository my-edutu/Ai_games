# Phase 7 — Serving and Product Integration

Build evaluation API/workflow that receives simulation run + artifacts and returns criterion scores, evidence, confidence, contradictions, coaching and reviewer status.

**Build:** async evaluation service, artifact retrieval, audit/version fields, reviewer override, latency/cost tests.

**Learn:** long-context/RAG systems, async jobs, audit trails.

**Exit:** every result records evaluator/rubric/scenario versions.