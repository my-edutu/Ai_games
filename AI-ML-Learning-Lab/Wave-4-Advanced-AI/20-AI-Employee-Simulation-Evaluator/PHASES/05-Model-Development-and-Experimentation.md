# Phase 5 — Model Development and Experimentation

Compare retrieval + structured LLM rubric evaluation, classifiers for selected signals and explicit cross-artifact reasoning. Require evidence citations for scores.

**Build:** prompt/model registry, structured evaluator, retrieval index, coherence checks, ablations.

**Learn:** RAG, structured outputs, LLM evaluation, ordinal models.

**Exit:** system improves human agreement/evidence accuracy without hiding uncertainty.