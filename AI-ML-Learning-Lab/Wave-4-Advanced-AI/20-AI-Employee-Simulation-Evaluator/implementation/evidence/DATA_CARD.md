# Data Card
Current evaluation uses deterministic representative task-template fixtures with observable competency features. Audit-group labels are separated from serving features. Real simulation artifacts, demographic audits, reviewer labels, and organization-specific task distributions are required before broad deployment.
