[project]
name = "simulation-evaluator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi", "pydantic", "numpy", "scikit-learn", "scipy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
