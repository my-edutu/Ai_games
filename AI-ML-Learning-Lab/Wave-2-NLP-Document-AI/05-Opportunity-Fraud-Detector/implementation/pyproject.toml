[build-system]
requires=['setuptools>=68']
build-backend='setuptools.build_meta'
[project]
name='opportunity-fraud-detector'
version='0.1.0'
dependencies=['pydantic>=2','fastapi','scikit-learn','numpy','pandas']
[project.optional-dependencies]
dev=['pytest','httpx']
[tool.pytest.ini_options]
pythonpath=['src']
