# Phase 3 — Data Pipeline and Exploration

Normalize scripts carefully without destroying meaning; inspect language balance, dialect, code-switching, length and label prevalence.

**Build:** ingestion, normalization tests, language-quality report, stratified splits, golden set.

**Learn:** Unicode, tokenization, dialect variation, imbalance.

**Exit:** preprocessing preserves original text and reversible provenance.