[build-system]
requires=['setuptools>=68']
build-backend='setuptools.build_meta'
[project]
name='african-language-moderation'
version='0.1.0'
dependencies=['pydantic>=2','fastapi','scikit-learn','numpy']
[project.optional-dependencies]
dev=['pytest','httpx']
[tool.pytest.ini_options]
pythonpath=['src']
