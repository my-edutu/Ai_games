# African-Language Moderation Design
Controlled multilingual moderation for `sw` and `pcm` with explicit `safe`/`harassment`/`threat` policy scope, per-language metrics, code-switch/context signals, uncertainty abstention, human escalation, fail-closed serving and native-speaker validation gate.
