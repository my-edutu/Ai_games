# Model Card
`rubric-calibrator-v1` is a Ridge calibration layer over transparent rubric/text features. It adjusts the aggregate coaching score only; evidence spans and dimension rationales remain deterministic and source-grounded. The committed benchmark is representative fixture evidence, not applicant-outcome validation.
