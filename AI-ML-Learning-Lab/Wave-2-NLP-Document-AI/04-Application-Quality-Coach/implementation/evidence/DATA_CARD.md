# Data Card
Development and benchmark data are deterministic representative reviewer fixtures. No real applications are committed. Production use requires consent, minimization, retention limits, redaction controls, and separation of application text from unnecessary identity fields.
