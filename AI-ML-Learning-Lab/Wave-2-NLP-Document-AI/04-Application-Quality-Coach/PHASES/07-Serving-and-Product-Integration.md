# Phase 7 — Serving and Product Integration

Expose rubric scores, evidence, prioritized feedback and optional rewrite suggestions through a typed API.

**Build:** API, streaming/non-streaming strategy, rate limits, version metadata, safety filters, tests.

**Learn:** LLM serving, structured outputs, caching, latency/cost trade-offs.

**Exit:** product can distinguish critique, suggestion and generated text.