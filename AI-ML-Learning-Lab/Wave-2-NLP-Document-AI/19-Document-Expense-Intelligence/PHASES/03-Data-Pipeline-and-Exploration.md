# Phase 3 — Data Pipeline and Exploration

Create image/PDF ingestion, deskew/quality checks, OCR/layout extraction and normalized field datasets.

**Build:** pipeline, OCR quality report, document-type distribution, split by vendor/template.

**Learn:** OCR, image preprocessing, layout structure, template leakage.

**Exit:** test set contains unseen vendors/templates where possible.