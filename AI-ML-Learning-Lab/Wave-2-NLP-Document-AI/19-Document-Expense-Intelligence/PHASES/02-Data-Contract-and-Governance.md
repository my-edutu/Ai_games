# Phase 2 — Data Contract and Governance

Define document, image/page, merchant, date, currency, subtotal, tax, total, line items, category and confidence schemas.

**Build:** schema, annotation format, storage/access policy, redaction rules, validation tests.

**Learn:** document annotation, financial PII, provenance.

**Exit:** every extracted field retains source document/page coordinates.