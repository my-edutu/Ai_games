# Phase 4 — Baseline and Evaluation Harness

Use OCR + regex/keyword heuristics as a transparent baseline. Evaluate exact/normalized field accuracy and monetary reconciliation.

**Build:** field precision/recall/F1, amount/date normalization tests, reconciliation metrics.

**Learn:** information extraction metrics, normalization.

**Exit:** baseline identifies difficult layouts/fields.