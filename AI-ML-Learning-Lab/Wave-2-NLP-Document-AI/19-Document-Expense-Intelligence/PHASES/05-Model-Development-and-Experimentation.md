# Phase 5 — Model Development and Experimentation

Compare improved OCR/layout models, token classifiers or multimodal document models; add spend categorization separately.

**Build:** tracked experiments, field-level ablations, category model, confidence calibration.

**Learn:** layout-aware transformers, token classification, multimodal models.

**Exit:** model beats baseline on unseen templates and critical monetary fields.