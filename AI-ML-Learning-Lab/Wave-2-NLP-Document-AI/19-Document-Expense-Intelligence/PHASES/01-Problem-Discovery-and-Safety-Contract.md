# Phase 1 — Problem Discovery and Safety Contract

Define supported document types, required fields, accounting outputs and when human approval is mandatory. Never silently invent missing monetary values.

**Build:** field taxonomy, workflow, error-cost matrix, metrics.

**Learn:** document AI pipeline design, extraction vs inference.

**Exit:** unknown/illegible values remain unknown.