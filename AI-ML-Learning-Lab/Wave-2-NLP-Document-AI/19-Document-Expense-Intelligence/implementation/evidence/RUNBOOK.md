# Runbook
Fail closed if parser/category artifacts are absent. Route low-confidence, duplicate, anomalous and reconciliation-conflicting entries to reviewers. Never overwrite source financial values silently.
