'use strict';
const fs=require('node:fs');const http=require('node:http');const path=require('node:path');
const{checksum}=require('../dist/packages/replay/src/index.js');
const{AntColonyRuntime,createAntRenderSnapshot,computeAntLayout,deriveAntCamera,deriveAntAudioCues,AntPresentationController}=require('../dist/games/ai-ant-colony/src/index.js');
const ROOT=path.resolve(__dirname,'..'),PUBLIC_ROOT=path.join(ROOT,'public','ai-ant-colony');
const STREAM_CONFIG=Object.freeze({width:64,height:40,surfaceRow:9,targetPopulation:180,initialWorkers:48,maxAnts:360,tickRate:10,intermissionTicks:35,strategyInterval:20,dayLengthTicks:500,seasonLengthTicks:2000,initialFood:180,initialWater:140,queenHealth:100,broodInterval:38,eggHatchTicks:24,larvaTicks:30,pupaTicks:26,foodPatchCount:12,foodRegrowInterval:60,foodRegrowAmount:4,predatorCap:5,predatorSpawnInterval:260,predatorDamage:5,soldierDamage:16,antMetabolismInterval:28,stuckRecoveryTicks:8,pheromoneDecay:1,pheromoneDeposit:14,tunnelCapacity:8,noProgressTicks:5000,profile:'forest'});
function createSession(seed='ant-colony-stream-reference'){const runtime=AntColonyRuntime.create(STREAM_CONFIG,seed),controller=new AntPresentationController();let current=createAntRenderSnapshot(runtime.state,runtime.drainEvents()),previous=null;controller.accept(current);return{runtime,controller,paused:false,get current(){return current},get previous(){return previous},setFrame(next){previous=current;current=next}}}
function advance(session){if(!session.paused)session.runtime.step();const next=createAntRenderSnapshot(session.runtime.state,session.runtime.drainEvents());const accepted=session.controller.accept(next);if(accepted.status==='accepted')session.setFrame(next);return accepted}
function assets(){return['index.html','styles.css','ux-v2.css','app.js'].map(name=>path.join(PUBLIC_ROOT,name))}
function privacySafe(snapshot,seed){const text=JSON.stringify(snapshot);return!('seed'in snapshot)&&!('config'in snapshot)&&!('influence'in snapshot)&&!text.includes(seed)&&!text.includes('appliedIds')&&!text.includes('lastCell')&&!text.includes('scheduledTick')}
function verifyRestart(){const session=createSession('ant-restart-probe');session.runtime.state.queen.health=0;advance(session);if(session.runtime.state.lifecycle!=='result')return false;advance(session);for(let guard=0;guard<STREAM_CONFIG.intermissionTicks+2&&session.runtime.state.lifecycle!=='active';guard++)advance(session);return session.runtime.state.lifecycle==='active'&&session.runtime.state.runIndex===1&&session.runtime.state.tick===0}
function selfTest(){const seed='ant-phase3-self-test',first=createSession(seed),second=createSession(seed);let accepted=0;for(let i=0;i<500;i++){const result=advance(first);second.runtime.step();if(result.status!=='accepted')throw new Error(`presentation rejection: ${result.status}`);accepted++}const snapshot=first.current,browserAssets=assets().every(file=>fs.existsSync(file)&&fs.statSync(file).size>128),source=browserAssets?fs.readFileSync(path.join(PUBLIC_ROOT,'app.js'),'utf8'):'',boundedSource=source.includes('MAX_EFFECTS=48')&&source.includes('MAX_VOICES=6')&&!source.includes('innerHTML'),report={ok:checksum(first.runtime.state)===checksum(second.runtime.state)&&browserAssets&&boundedSource&&privacySafe(snapshot,seed)&&verifyRestart()&&accepted===500,authorityStable:checksum(first.runtime.state)===checksum(second.runtime.state),browserAssets,boundedSource,snapshotPrivacySafe:privacySafe(snapshot,seed),restartObserved:verifyRestart(),acceptedSnapshots:accepted,finalTick:first.runtime.state.tick,scene:snapshot.scene,revision:snapshot.revision};process.stdout.write(`${JSON.stringify(report)}\n`);process.exitCode=report.ok?0:1}
function contentType(file){if(file.endsWith('.html'))return'text/html; charset=utf-8';if(file.endsWith('.css'))return'text/css; charset=utf-8';if(file.endsWith('.js'))return'text/javascript; charset=utf-8';return'application/octet-stream'}
function headers(type,length,html=false){return{'content-type':type,'content-length':length,'cache-control':html?'no-store':'public, max-age=300','content-security-policy':"default-src 'self'; script-src 'self'; style-src 'self'; connect-src 'self'; img-src 'self' data:; media-src 'none'; object-src 'none'; base-uri 'none'; frame-ancestors *",'referrer-policy':'no-referrer','x-content-type-options':'nosniff','cross-origin-resource-policy':'same-origin'}}
function sendJson(response,status,value){const body=JSON.stringify(value);response.writeHead(status,headers('application/json; charset=utf-8',Buffer.byteLength(body),true));response.end(body)}
function sendFile(response,file){if(!fs.existsSync(file)){response.writeHead(404);response.end('Not found');return}const body=fs.readFileSync(file);response.writeHead(200,headers(contentType(file),body.length,file.endsWith('.html')));response.end(body)}
function parseJson(request,limit=2048){return new Promise((resolve,reject)=>{let text='';request.on('data',chunk=>{text+=chunk;if(Buffer.byteLength(text)>limit){reject(new Error('body-too-large'));request.destroy()}});request.on('end',()=>{try{resolve(text?JSON.parse(text):{})}catch{reject(new Error('invalid-json'))}});request.on('error',reject)})}
function serve(){const argument=process.argv.find(value=>value.startsWith('--port=')),port=Number(argument?.split('=')[1]??process.env.PORT??4175);if(!Number.isInteger(port)||port<1||port>65535)throw new RangeError('port');const session=createSession(process.env.ANT_COLONY_SEED||'ant-colony-stream-reference'),operatorToken=process.env.ANT_OPERATOR_TOKEN||'',stepMs=Math.max(25,Math.round(1000/STREAM_CONFIG.tickRate));let lastStepAt=Date.now(),simulationFault=false;const interval=setInterval(()=>{try{advance(session);lastStepAt=Date.now();simulationFault=false}catch{simulationFault=true}},stepMs);interval.unref();const server=http.createServer(async(request,response)=>{const url=new URL(request.url||'/','http://local.invalid');if(url.pathname==='/ant/state'){const width=Math.max(320,Math.floor(Number(url.searchParams.get('w')||1920))),height=Math.max(180,Math.floor(Number(url.searchParams.get('h')||1080))),snapshot=session.controller.recover()||session.current;sendJson(response,200,{snapshot,layout:computeAntLayout(width,height,url.searchParams.get('cleanFeed')==='1'),camera:deriveAntCamera(snapshot,session.previous),audioCues:deriveAntAudioCues(snapshot,session.previous),status:{paused:session.paused,simulationFault,lastStepAgeMs:Math.max(0,Date.now()-lastStepAt)}});return}if(url.pathname==='/ant/health'){const snapshot=session.controller.recover()||session.current;sendJson(response,simulationFault?503:200,{status:simulationFault?'degraded':'healthy',lastStepAgeMs:Math.max(0,Date.now()-lastStepAt),scene:snapshot.scene,tick:snapshot.tick,paused:session.paused});return}if(url.pathname==='/ant/command'&&request.method==='POST'){if(!operatorToken){sendJson(response,503,{error:'operator-token-not-configured'});return}if(request.headers['x-ant-operator-token']!==operatorToken){sendJson(response,403,{error:'unauthorized'});return}try{const body=await parseJson(request),command=body.command;if(!['pause','resume','restart'].includes(command)){sendJson(response,400,{error:'unsupported-command'});return}if(command==='pause')session.paused=true;else if(command==='resume')session.paused=false;else{session.runtime.restart();session.controller.clear();session.setFrame(createAntRenderSnapshot(session.runtime.state,session.runtime.drainEvents()));session.controller.accept(session.current)}sendJson(response,200,{ok:true,command,runIndex:session.runtime.state.runIndex,tick:session.runtime.state.tick});return}catch(error){sendJson(response,400,{error:error instanceof Error?error.message:'invalid-command'});return}}if(url.pathname==='/favicon.ico'){response.writeHead(204);response.end();return}const files={'/':'index.html','/ant':'index.html','/ant/':'index.html','/ant/index.html':'index.html','/ant/styles.css':'styles.css','/ant/ux-v2.css':'ux-v2.css','/ant/app.js':'app.js'},name=files[url.pathname];if(!name){response.writeHead(404,{'content-type':'text/plain; charset=utf-8'});response.end('Not found');return}sendFile(response,path.join(PUBLIC_ROOT,name))});server.listen(port,'0.0.0.0',()=>process.stdout.write(`AI Ant Colony stream source listening on port ${port}\n`));const shutdown=()=>{clearInterval(interval);server.close(()=>process.exit(0))};process.on('SIGINT',shutdown);process.on('SIGTERM',shutdown)}
if(process.argv.includes('--self-test'))selfTest();else serve();
